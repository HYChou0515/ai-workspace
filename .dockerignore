# Keep the build context small + reproducible (the image rebuilds deps/SPA).
.git
.venv
# view-plugins/<name>/{sandbox-src/.venv, web/node_modules} are dev envs too:
# the image builds each plugin from its lockfiles.
**/.venv
**/__pycache__
**/*.pyc
.pytest_cache
.ruff_cache
.coverage
htmlcov
node_modules
**/node_modules
web/node_modules
web/dist
docs
design_handoff_rca_3.0
kubernetes
*.md
!README.md

# Observability runtime artifacts (resolved-config dump + LLM call log).
config.resolved.yaml
logs
