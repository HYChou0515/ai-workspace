# Keep the build context small + reproducible (the image rebuilds deps/SPA).
.git
.venv
**/__pycache__
**/*.pyc
.pytest_cache
.ruff_cache
.coverage
htmlcov
node_modules
web/node_modules
# #847/#848: a view plugin's own install (view-plugins/*/web/node_modules) too —
# a bare pattern matches the context root only.
**/node_modules
web/dist
docs
design_handoff_rca_3.0
kubernetes
*.md
!README.md

# Observability runtime artifacts (resolved-config dump + LLM call log).
config.resolved.yaml
logs
