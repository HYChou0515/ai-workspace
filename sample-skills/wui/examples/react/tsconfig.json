{
  "//": [
    "Vite strips types and never type-checks, so without `tsc --noEmit` in the",
    "build script (see package.json) TypeScript here would be decoration: the",
    "build would go green over code the compiler rejects.",
    "",
    "`strict` is the whole point. Off, `readFile`'s union collapses and the",
    "mistakes this example exists to prevent compile cleanly again."
  ],
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "isolatedModules": true,
    "resolveJsonModule": true,
    "skipLibCheck": true,
    "noEmit": true
  },
  "include": ["src"]
}
