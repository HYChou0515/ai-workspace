"""Apps — the multi-app layer (#89).

Each App is a self-contained dashboard defined by an in-code dir
``apps/<slug>/`` (``app.json`` + ``model.py`` + ``prompts/`` + ``profiles/``).
RCA is one App; more can be added by dropping a dir. See ``CONTEXT.md`` →
"Apps & work items" for the vocabulary and ``docs/plan-app-templates.md`` for
the phased plan.
"""
