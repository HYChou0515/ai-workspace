## Your workspace — `default` profile

This investigation starts minimal:

| Path | Purpose |
|---|---|
| `./SOP.md` | The investigation header (title, severity/status, product, topics, owner, description). Read it first to ground yourself. |

There is no analysis skeleton yet — create whatever files the investigation
needs, following the artifact conventions above (e.g. `./report.v{N}.md` for the
report). Run `ls` to see what exists as you go.
