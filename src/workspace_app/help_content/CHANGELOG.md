# 更新紀錄 / Release notes

本頁記錄平台面向使用者的重要更新,格式採 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)。
新版本區段由 [git-cliff](https://git-cliff.org/) 從 Conventional Commits 自動生成,並隨程式碼一起
進版控,所以每次部署都會是最新的。請勿手改已生成的版本區段——它們會在下次 `make release` 時重新產生。

This page records notable user-facing platform updates in the
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/) format. New version
sections are generated by git-cliff from Conventional Commits and ship with the
code, so the running version is always current. Do not hand-edit generated
sections — they are regenerated on the next `make release`.

---
## [2026.07.06] — 2026-07-06


### Added

- SpecstarFileStore on per-workspace blob model
- Add Workspace / AgentConfig / Conversation Structs
- Tool wrappers over Sandbox + FileStore
- FastAPI app factory + SSE message endpoint
- Add LocalProcessSandbox + DockerSandbox adapters
- LitellmAgentRunner — agents SDK + LiteLLM/Ollama
- React SPA chat page + entrypoint + CLAUDE.md docs
- Retry-with-feedback loop for small-model tool-call errors
- WorkspaceRegistry — sticky per-workspace session state
- FS↔Sandbox bidirectional sync + Files API
- Interrupt support — DELETE endpoint + cancel-on-new-POST
- Idle-kill lifecycle — background reaper + lifespan teardown
- Refined SSE event variants — ToolCallParseError + MaxTurnsExceeded
- Seed default template on POST /investigation
- System prompt + default_rca_agent_config
- Idle 8h default + POST /investigations/{id}/close
- PUT /investigations/{id}/files/{path} for raw byte upload
- Default Dockerfile + jupyter_client/ipykernel deps
- Expose_port Protocol + 3 adapter impls
- KernelService + CellEvent types
- Notebook cell execute + interrupt + kernel restart
- RCA 3.0 implementation per plan-frontend.md §12.1–§12.10 + interactive polish
- Bottom panel reads agent log + run-all cells + attach + Esc + BE wire-format
- Wire close/cancel/interrupt/restart + Close button
- POST /investigations/{id}/exec + MemoryFileStore default
- Finish v1 polish — restart, upload, settings, split, terminal
- DELETE /files/{path} + POST /files/move
- Profile-based seeding + .tpl substitution
- GET /activity recent-activity feed + event hooks
- Shared per-path file buffers for live split-pane sync
- Monaco editor — syntax highlight, line numbers, ⌘F, minimap
- Resizable + collapsible panels (⌘B / ⌘J)
- ⌘W close tab + ⌘1-9 jump to Nth tab
- VSCode tab behaviors — preview / pin / drag / context menu
- Collapsible file tree + right-click menu
- Working light / dark / system theme
- Template picker in New Investigation modal
- Breadcrumb redesign — topics > product > title + filters
- Notifications popover backed by GET /activity + unread badge
- Multi-pane editor + inline file tree create/rename
- Edge-drop panes, file-tree drag move/copy, sidebar trim
- Real VSCode editor groups + fix file-tree drag highlight
- Search/replace, agent-config routing, pure close
- Agent-config picker + pure-close menu
- VSCode-style global search + replace panel
- Clickable breadcrumb with sibling dropdowns
- Honest directories in the FileStore + endpoints
- Real folders in the file tree
- Explicit-save buffer model (no autosave)
- Imperative confirm Dialog (useDialog)
- Dirty tabs, ⌘S save, save-on-close, middle-click close
- File-tree multi-select + delete-open-file handling
- Replace prompt on move/copy name collision
- Replace prompt on rename + new-file/folder collisions
- Every file is editable, even binary (lossless byte codec)
- Agent quick-prompt suggestions live on AgentConfig (BE)
- Home table — no wrapping, Topics column, reordered columns
- Templates gallery (sidebar Templates → pick → New Investigation)
- Enter-to-send + markdown-rendered agent replies
- Fuller Agent Log + Run History panels
- Terminal no longer freezes on interactive programs
- Live token telemetry (↑/↓ tok/s) + richer run history
- Render LaTeX; fix final token reset; ellipsis truncated args
- Replace default profile skeleton with a single SOP.md
- Real isolation via user-namespace + chroot jail
- Live tool output streaming in run history
- File-tree refresh, folded+timestamped agent log
- Persist message timestamps + tool name/args so the log survives reload
- Frozen knowledge-base resource model + registration
- Chunker protocol + FixedTokenChunker
- Embedder protocol with asymmetric query/doc prefixes
- Document ingestion (upload → SourceDoc + embedded chunks)
- Cross-reference markdown link rewriting (pure)
- Collection/document/render API routes + wire into create_app
- Rank fusion (RRF) + MMR diversification
- BM25 sparse keyword scorer
- Parent-document text-merge of retrieved chunks
- Hybrid Retriever core (dense + sparse + RRF + MMR + merge)
- Multi-query expansion (LLM) wired into the Retriever
- Kb_search agent tool + KB-flavoured AgentToolContext
- Parse [n] answer markers into Citations
- Default KB AgentConfig + system prompt
- KbChat threads + SSE chat endpoint (agentic, with citations)
- Ask_knowledge_base — RCA agent consults the KB agent
- KB API client (collections, documents, chats, SSE chat)
- UseKbChat hook — stream a KB turn, refetch for citations
- Ask-agent KB drawer (streaming answer + citation cards)
- KB doc viewer + kb:// link resolver (citation target)
- KB collections management page (create, upload, list docs)
- KB shell — chats history, /kb route, Home entry
- HyDE probe + LLM rerank in the Retriever
- Persist KB tool-call messages in the chat thread
- In-text citation highlight + render persisted tool calls
- Real semantic embedding + activate retrieval LLM in the live app
- Expose document created_by (specstar audit meta) in the list
- KB agent quick-prompt suggestions + GET /kb/agent
- KB UX restructure — fast-chat drawer vs full chat views
- Folder upload — ingest a directory like an archive
- Async ingestion — store fast, embed in the background
- Filter documents within a collection by name
- Show kb_search results as a distinct panel (not just a chip)
- KB chat matches the RCA agent (reasoning/tools/metrics)
- Make the KB agent cite [n] more reliably
- Brand entry animation, theme-aware logo, real Settings
- Container image, k8s (base+overlays), GitLab CI, sub-path deploy
- Edit an investigation's title/description/severity/product/topics
- Fetch current user id via api.getCurrentUser (mocked until SSO)
- Add TanStack Query infra + migrate currentUser
- Migrate investigation reads + writes to TanStack Query
- Migrate agentConfigs/templates/activity/fileContent to useQuery
- Migrate KB reads + writes to TanStack Query
- Hydrate agent/KB chat threads via TanStack Query
- Stream the KB agent's intermediate state (#4)
- KB chat interrupt — wire the server cancel (parity with RCA)
- P1 — identity (UserDirectory) + notifications foundation
- P2 — CitationEvent log + chunk/doc/collection cited counts
- P3 — read-only chat sharing + shared-with-me
- P4 — @mention summon (human + agent), decoupled from the agent
- P5 — KB FE redesign (cited surfacing, chunks view, home nav, drawer)
- KB collection list KPI strip (count + most-cited)
- Set root_path on the FastAPI app, not just uvicorn.run
- Document Size + Updated columns (specstar Binary.size + updated_time)
- P1 — WorkspaceFiles facade + structured file ops + FileEntry.version
- P2 — flip source of truth to the live sandbox
- P3 — durable throttled mirror (version-diff, deletion-aware)
- P4 — edit_file + create-only write_file with compare-and-swap
- #11 — live LLM/agent telemetry monitor on the SDK trace stream
- Add SpecstarMonitor + replace fetch-all-then-filter with indexed queries
- #10 — surface the KB retriever's enhancement-LLM thinking in chat
- Collection card grid matching the handoff + typed responses
- CollectionPage detail — pickable icon, stats banner, rename/delete
- Aligned doc table + menu-based collection actions + reindex-all
- Upgrade doc drawer — meta header + action bar (open/download/reindex/remove)
- Collections landing — rich header, tabs, filters; editable description; scroll
- KB chats — list tabs + rich conversation header (share/pin/export)
- Align Investigations home header to design
- Stick-to-bottom for KB chat + reasoning + tool-log panes
- Keep cumulative token status visible (paused) during tool calls
- Sandbox tool provisioning — install per-tool deps into the sandbox
- #22 persist final token metrics on the assistant message (BE half)
- #21 wire sandbox tool provisioning into the runner
- Tool-demo profile to smoke-test provisioned tools with an LLM
- Export an investigation's full conversation as JSON
- Copy a prebuilt relocatable venv into the sandbox
- Bind a template profile to its own AgentConfig
- Wire sample tools into the normal entry; drop the launcher
- Self-contained tool packages that run in the chroot jail
- LLM base_url + api_key (chat + separate embedder)
- --plot (distributions + correlation heatmap)
- Mount provisioned tools read-only at /.tools (no per-sandbox copy)
- CSV table preview
- Per-message reasoning-effort selector (RCA + KB chat)
- Multi-replica k8s — RWX storage, replicas, per-investigation affinity
- Extensible preview registry + html/bmp preview types
- Split-pane resize
- Cross/T handle + linked perpendicular splits
- T-junction cross handle for split panes (VSCode-style)
- P1 — LlamaIndex IngestionPipeline for doc ingest
- P2 — chat → knowledge insight extraction
- Route code files (.py/.ts/.tsx/.js/.jsx) through CodeSplitter
- CodeRepoIngestor — clone git remote + ingest tracked files
- Per-collection embedder + dual-vector DocChunks (D1)
- Retriever fan-out across embedding + embedding_alt
- Wire kb_code_embedder + POST /kb/collections/:id/sync
- CodeRepoSweeper + lifespan-driven background sync
- Dispatcher — 3-stage CLI contract helper for tool authors
- Prebuild.py — build self-contained tool packages
- Registry.py — discover prebuilt bundles + build FunctionTools
- Migrate data-fetch to 3-stage JSON contract
- Migrate csv-column-summary to multi-command package
- Tool-demo template + end-to-end integration
- Skills — host-side progressive disclosure for RCA agent
- Compose_system_prompt appends Available skills index
- Read_skill tool + template_profile context plumbing
- Thread template_profile through runner + app
- Seed methodology template with 3 RCA skills
- Final §A skill gate + plan check-off
- P1 — WorkflowRun resource (#100)
- P2 — filesystem-as-journal step engine core (#100)
- P3 — WorkflowManifest + profile workflow discovery (#100)
- P2 — agent_step + sandbox_node node adapters (#100)
- P4 — orchestration driver status lifecycle (#100)
- P5 — ingest_to_collection capability core (#100)
- P6 parallel map + P11 human_gate (#100)
- P4–P11 — Run endpoints, orchestrator, credential, events, robustness, lifecycle (#100)
- P12 frontend — run progress view, decision card, phase diagram (#100)
- P1 — ContextCard resource + norm() deterministic core (#106)
- P2 (backend) — context-card create/update custom actions (#106)
- P2 (frontend) — Context Cards tab on the collection page (#106)
- P3+P5 — context-card lookup + match primitives (#106)
- P4 — external context-card lookup API (#106)
- P6 — inject matched context cards into the KB chat turn (#106)
- Context card search (by name / in text) + scrollable list (#106)
- Context card — preview by default + compact dense list (#106)
- Context card — Delete only shows in Edit, not Preview (#106)
- Context card — fall back to title as key when no key given (#106)
- P1 — hyphenated-slug file-path App loader + skeleton
- P2 — collections.json helpers + resolve_collection tool
- P3 — lookup_glossary context-card tool
- P4 — context_files manifest field + injection helper
- P5 — multiple workflows per profile (manual §4)
- P6 — multi-chat data model + default-chat back-compat (§3)
- P7 — chat-scoped endpoints (§3)
- P8 — workflow-chat launch + parallel runs (§3, §3.1)
- P9 — create_context_card capability (§8)
- P10 — the composed Topic Hub App (§9–§11)
- P11 — →memory workflow (§12)
- P12 — →collections workflow (§12)
- P13 — →consolidate workflow (§12)
- P14a — FE multi-chat data layer (api + hooks)
- P14b — FE multi-chat shell components (§3)
- P14c — mount the multi-chat shell (gated to topic-hub) (§3)
- Rich Run-workflow picker + real linear step bar (§4, §12)
- WORKSPACE_AGENT_STREAM=0 escape hatch — non-streaming turns
- Structured decide-then-act Model (opt-in) at the SDK Model seam
- Stream the final answer in decide-then-act (only tool-call gen stays non-stream)
- Provider-aware reasoning-off (Ollama think=False, others vLLM enable_thinking)
- Make lookup_glossary available in KB chat (term → glossary, question → kb_search)
- Chunk Markdown tables by row, spanning the whole table (#116)
- Wheel-zoom + drag-pan in the image viewer
- Repetition-loop detector — Phase 1 of #113
- Wire repetition guard into every turn — Phase 2 of #113
- Per-preset anti-repetition sampling penalties — Phase 3 of #113
- Repetition-stop notice + persisted flag — Phase 4 of #113
- Add read_image tool calling the VLM over workspace images (#112)
- Let the agent and workflows update context cards (#111)
- Collapse the workspace for a full-width chat + fix drag-break
- Collection-set picker UI (#142)
- Legible never-freeze turn-wait status
- Live run activity, no-op reasons, and fix chat export
- Show loading while HTTP requests are in flight (#151)
- Expand truncated sanity-matrix output into a read-only modal
- Make KB views URL-addressable (#93)
- #162 IA explainers + collection index-status strip
- Collection export/import backend (#101)
- Collection download/import UI (#101)
- Global nav bar + breadcrumbs (#158)
- #160 add simple i18n + de-jargon the Settings panel
- #160 de-jargon the model + reasoning-depth picker
- #160 de-jargon agent tool cards + activity copy
- #160 de-jargon the launcher cards + KB shell
- #160 de-jargon agent run banners + add i18n interpolation
- Explain collection concepts in place (#173)
- #172 surface core actions (upload/undo/nav/collections/re-index/filters)
- Cap kb_search calls per KB-chat turn (#195)
- Busy-aware LLM failover core + config + KB LLM/VLM wiring (#196 #131)
- Embedder same-model replica failover + failover_call (#196)
- Agent / sub-agent path busy-aware failover (#196)
- Operator switch logging + config docs + live smoke test (#196 #131)
- Per-step status board — alive vs dead per step (#178)
- Show context-card diff at the →collections review (#205)
- One-click re-index of only the failed docs (#223)
- Single-chat-leaning multichat for every App workspace (#200)
- Accept file uploads in the run trigger (#197)
- HttpSandbox client + FastAPI host wire (#60 P1)
- IsolatedProcessSandbox — per-handle uid + cgroup (#60 P2)
- Wire HTTP sandbox config + host entrypoint (#60 P3)
- Host ops — drain, idle-reaper, health/readiness (#60 P4)
- Adjustable system font size (#226)
- Per-file Binary workspace store + inline-bytes migration (#219 P1-P2)
- End-to-end streaming upload + big-file support (#219 P3-P6)
- Workspace-aware + defensive base preamble; rename ls→list_files (#241)
- Download files & folders from KB and workspace (#247)
- Bundle ppt/excel libs in python-stack; deprecate DockerSandbox (#252)
- Expose monotonic fan-out unit progress for the doc list (#248)
- Draw a monotonic fan-out progress bar in the doc status bar (#248)
- Retain page/section provenance so chunks have big-picture context (#254)
- Profile-configurable upload folder + folder/big-file chat attach (#198)
- In-process transient-error retry for index-time model calls (#249)
- Specstar job-layer transient retry for index jobs (#249)
- VLM describe is one retryable unit (#249)
- Surface a live failover-switch notice to the FE (#249/#131)
- Show an ephemeral "model switched" notice on chat failover (#249/#131)
- 自動 context card generation (#175) (#268)
- Attribute user messages to their author in LLM history (#242 P1)
- Tell the agent who it is replying to via a per-turn system note (#242 P2)
- Attribute KB chat messages to their sender (#242 P3)
- Per-workspace storage quota + blob GC (#245)
- Structural provenance filter for kb_search (#263)
- AI judge for sanity cells + ai_grade/ai_note (#231 P1-P2)
- Per-model fitness verdict (#231 P3)
- Coverage run-missing + rescore endpoints (#231 P4)
- User-authored custom questions, AI-only graded (#231 P5)
- Typed custom-question CRUD endpoints (#231 P8 backend)
- Coverage table + verdict cards + question manager FE (#231 P6-P9)
- Ranked collection pills + shared picker modal for KB chat (#271)
- Permission model + central authorize() (#262 P1)
- Extensible scientific plotting catalog + VLM self-review (#285)
- Make_deck — designed pptx via a multimodal sub-agent loop (#284)
- Pre-flight preview backend (#283 P1)
- Pre-flight hooks for topic-hub workflows (#283 P3)
- Pre-flight launch dialog (#283 P2)
- Interactive timeline view + metrics + view toggle (#283 P5/P6)
- Runs as a first-class, browsable list (#283 P7)
- Authoring DX — scaffold + check CLI + guide (#287)
- Lookup_user tool resolves a teammate by handle (#275)
- Conversational steer + incremental resume (#288)
- Score document quality + down-weight low-quality docs in search (#105)
- Load + advertise per-workspace skills (#298 P1)
- Save_skill tool for deterministic SKILL.md writes (#298 P2)
- Shared-skill registry + agent.skills opt-in (#298 P3)
- Author-skill meta-skill + opt-in rca/topic-hub/playground (#298 P4)
- GET .../skills endpoint listing workspace skills (#298 P5 backend)
- Skills panel in the agent header (#298 P5 FE)
- Tiered, web-editable collection scope for RCA (#280)
- Collection read/write visibility via specstar access_scope (#262 P2)
- Per-verb Collection write checker (#262 P2)
- Permission-set endpoint, content guards, superusers wiring (#262 P2)
- Seed platform Help collection from packaged content at boot (#230 P1)
- GET /help endpoint — Help collection id + documents (#230 P2)
- /help page + entry points; lockable KB chat (#230 P3-P5)
- Block encrypted/unreadable uploads with pluggable checks (#325)
- Per-item web tool picker + backend tool catalog (#322)
- Restore rich run progress in workflow chats (#331)
- P1 — L0 code-wiki file cards (outline + LLM one-liner)
- P2 — L1 directory roll-up pages
- P3 — L2 architecture, index & topic pages
- P4 — wire code-wiki build into the ingest→wiki coordinator
- P5 — _unfence polish, docs, DoD
- P1 — wire the code-wiki build trigger onto the real sync flow (A0+A1)
- P4 — job-queue fan-out for the code-wiki build (Q1+Q2)
- P5 — per-page input-hash incremental L1/L2 (Q3)
- P7 — robust _unfence + read_source tolerates the wiki-card path (B8)
- P6 — code-wiki build phases + rebuild hint in WikiBrowser (Q5, minimal)
- P1 — collection parser_guidance append seam
- P2 — dry-run re-parse helper (non-persisting)
- P3 — deep-rank retrieval (search depth param)
- P4 — findability probe service + endpoint
- P5+P6 — findability probe FE (client, types, modal, wiring)
- P7 — docs + ty/lint cleanup
- Configurable, resilient failover so a busy system doesn't kill the turn (#196)
- P1 — sandbox scratch-vol cap + per-item-user isolation config
- P2 — stable per-item sandbox id + cross-pod dir resolution
- P3 — shared-dir cross-pod routing + restore-when-absent
- P4 — recycle a shared dir only when GLOBALLY idle
- P7 — k8s two-vol split (durable archive + shared scratch)
- P5 — scratch-vol du soft quota sweeper
- P6 — per-item OS-user + cgroup isolation (shared-vol local)
- Cross-pod turn cancellation via a shared specstar epoch
- P1 — daily code-collection auto-sync gate
- P2 — code_sync wiki-job op (clone+ingest on the worker)
- P3 — async /sync + sweeper enqueues code_sync
- P4 — code-repository mode in the New collection modal
- P5 — first-sync on create + collection-page sync status strip
- P6 — edit a code collection's git connection
- Tune parsing — per-doc prompt escape hatch + answer preview
- Teach workspace agents how users upload files
- Paste + drag-drop file/image upload UX
- Structured-data viewers for json/jsonl/csv/tsv/yaml
- P1-P2 sandbox address in specstar + self-heal on dead handle
- P3 .ready marker + mirror sandwich (deletes only on a complete sandbox)
- P4 sandbox-host reap unlinks .ready before rmtree
- P5 readiness becomes a first-class out-of-workspace marker
- P6 switch sync to mark_ready/is_ready, drop the file marker
- P7 make the reaper/mirror sweeps resilient per item
- P8 wire the address store for http + docs
- KB chat list name_hint + updated_ms, normalize unnamed default
- PATCH /kb/chats/{id} to rename a chat (owner-only)
- KB chat list labels unnamed chats + recency sort (FE)
- Inline pencil rename in the KB chat list (FE)
- KB chat-view header + export label unnamed threads by name_hint
- P1 — take over the current chat to run a workflow
- P2 — keep the taken-over chat coherent
- P3 — launch a workflow in the current chat from its header
- Per-item skill enable/disable + profile.skills default (#380 P1)
- Skills picker state endpoint + single-source resolver (#380 P2)
- Apply-a-skill-this-turn + live toggle wiring (#380 P3)
- Skills panel lists all sources + tri-state toggle + apply (#380 P4 FE)
- Compose applied-skill chips + thread applySkills through the turn
- P1 DocQuestion resource + lifecycle helpers
- P2 digest raises questions instead of hallucinating
- P3 per-collection auto_digest toggle + index hook
- P4 land a term answer as a context card
- P5 land a description answer on a builder-immune wiki page
- P6a global inbox routes (list / answer / discard)
- Global clarification inbox FE page
- Re-run idempotency, edge cases, 100% gate
- Add Embedder.identity for the #390 index cache key
- Add IndexCache + CachedChunk resource for #390
- Compute_cache_key for the #390 index cache
- IndexCacheStore + Ingestor cache ops for #390
- Write the index cache on successful indexing (#390)
- Wire the index cache into the KB routes (#390 P6+P7)
- Index status/status_detail/content_type + IndexRun progress (#395 P1)
- Documents/status — the cheap poll target for indexing (#395 P3)
- Poll the status summary, not the whole doc list (#395 P4)
- Opt rca/playground/_template into author-workflow + save_workflow (#400)
- Surface workspace-authored workflows in the in-chat launch menu (#400)
- Doc filetree filter + resizable tree width (#405)
- P1 folder-ize the builder-immune ground truth
- P2 submit_wiki_correction + correct op + corrector agent
- P3 request_wiki_update agent tool + wiring
- P4 correction routes + adaptive AI drafting assist
- P5 KB chat "回報有誤" button + AI drafting dialog
- Emit mirror/restore telemetry into IMonitor
- Emit blob_gc GcStats + ws_census telemetry
- Kind-indexed telemetry + GET /monitor/summary
- Durable-store summary card on the telemetry panel
- CardGenRun/CardGenUnit + CardGenRunStore CAS join state
- Fan out card generation across per-doc process jobs
- P1 — FileTree opt-in controlled multi-select mode
- P2 — card-gen picker on a docs+wiki multi-select filetree
- P3 — resolve picked wiki pages as card-gen sources
- P4 — 待審核 tab, review inbox shell, run resolution states
- P5 — fold clarification questions into the inbox; unify #377
- Collection sharing UI — generic role-based permission dialog
- Logical Group entity (minimal viable)
- Ask_knowledge_base transitive read_content check
- App WorkItem resource-layer permission enforcement
- KbChat resource-layer permission enforcement
- Agent-tool authorization — the AI is bounded by ceiling ∩ speaker
- Entity CRUD spine (P1 backend)
- Relational roles + compute-on-read projection (P2 backend)
- Entity CRUD API routes (P1 wiring)
- Entity CRUD as a workflow capability (P3)
- Ship the Project-Management app (P4 — declarative entity bundle)
- Entity view renderers — board / table / gantt (P1 + P2 FE)
- SourceDoc read access inherits its collection's visibility
- Agent-facing entity tools — the AI write path (P5)
- Optimistic version check on entity update (P6, C6)
- Project-health view (P7, E3)
- Views-first main surface (P8, B5)
- Exclusive-create numbering arbiter (P10, N1)
- Entity collaboration — activity feed + assignment notifications (P9)
- P1 — unified reference model {steps.<name>.<field>}
- P2 — outputs schema (types + enum + retry-on-mismatch)
- P3 — switch conditional node (the one branch primitive)
- P4 — map `over` list value / range
- P5 — fan-in {steps.<map>.outputs} + rewrite file-uploads.json
- P6 — gate revise (bounded human-gated back-edge)
- SourceDoc.permission override field + Collection.has_doc_overrides (P1)
- Source_doc access_scope intersects a per-doc read override (P2)
- Enforce a per-doc read override on the content routes + doc list (P3)
- Doc-permission write API + auto-CRUD anti-bypass + denylist (P4)
- Enforce per-doc override on AI retrieval + collection export (P5)
- Per-doc share dialog in the doc IDE (P6, FE)
- P1 content-aware cache — reads field + stale-risk lint
- P2 update_entity DSL capability (optimistic-retry)
- P4 journal-orphan lazy GC for shrinking maps
- P5 per-element sub-handle real parallel + card optimistic upsert
- P6 declarative trigger schema + static validation
- P7 time-trigger runtime — sweeper + CAS lease + start
- P8 orphan pickup (resume) + abandon expiry for triggered runs
- P9 event triggers — fire a run when an entity changes
- P1 — non-idempotent capability shell (decide/act as two run_steps)
- P2 — create_entity self-dedup on the shell (fixes revise double-create)
- P3 — M1-AI cross-origin dedup + non-destructive fenced-region cross-merge
- P4 — create_new (M2 token), gated until #429
- P5 — send_notification (M1 send-once over the in-app Notification store)
- P6 — wire decide-AI over the run model + live check + docs
- P7 — create_new cross-invocation (M2 token over run_id)
- P8 — send_notification per-window fingerprint
- P10+P11 — close the event-dispatch seams (agent-write origin + D2d backfill) (#444)
- P2 — useEntityWrite single write seam (optimistic + 409 conflict + canWrite + SSE seam)
- P3 — single role→widget table wired into table / board / quick-create
- P4 — ref-traversal helper + ref #N-title picker
- P5 — table sort / value-filter / column show-hide (local, ephemeral)
- P6 — layered fault-tolerant degradation + Diagnostic marks (§D)
- Table multi-select + batch status/actor (fan-out N updates)
- PM gantt interaction — drag-reschedule / zoom / swimlanes
- P1 — HealthView filter bar + click-to-jump onJump contract (§F)
- P2 — jump-to-record via OpenFile context (§F)
- PM board renderer interactions — drag-to-move + card face + out-of-vocab columns (#463)
- P1 — entity update route carries an optional markdown body (§C2)
- P2 — entitiesApi.update + useEntityWrite carry the markdown body (§C2)
- P3 — EntityFileEditor (frontmatter form/YAML toggle + markdown body)
- P4 — register RecordFileRenderer (issues/N.md opens the file editor)
- P5 — body + raw YAML ride the shared Monaco IDE stack
- P1 — read-only gate (canWrite) across entity views
- P2 — live-sync entity writes (broadcast + FE refetch)
- P3 — per-item activity feed (collaboration timeline)
- P4 — live presence (who else is viewing this item)
- P1 — reactive breakpoint foundation (useMediaQuery + useIsNarrow)
- P2 — KB shells collapse to single column below 768px (CSS only)
- P3 — AppDashboard stacks its 240px sidebar below 768px
- P4 — investigation shell goes single-column below 768px
- P5 — global breadcrumb bar shrinks + clips instead of overflowing
- P1 — git-cliff changelog machinery (cliff.toml + Makefile + CalVer)
- P2 — publish a GitHub Release on CalVer tag push
- P3 — parse CHANGELOG into a typed /help/releases endpoint
- P4 — /help/releases per-version release-notes view


### Changed

- Rename Workspace → Investigation + RCA pivot model
- Composition-root factories (Settings + get_*(settings) -> Protocol)
- Shared ChatTurnEngine for RCA + KB chat turns
- Workspace is ~ (a subdir); tools/caches live outside it
- Unify Home + KbHome "Ask agent" on AskAgentLauncher
- ResizeDivider anchors on drag start (fixes drift)
- Replace ToolDef with PackageInfo throughout
- Split HTTP sandbox host into a standalone service (#251)
- Share input.json resolution between run + preview (#283 P9)
- Consolidate cross-app sandbox + security guidance into shared preambles
- One react-query content cache under the file buffer (PR-2/3)
- P1 — split entity renderers behind a viewKindRegistry
- Migrate WorkspaceShell edit-item modal to <ModalShell>


### Documentation

- Add README with Ollama setup walkthrough
- Full implementation plan (what's done + what's left)
- Split implementation plan into backend + frontend
- Pivot plans to RCA 3.0 vertical app
- Collapse RCA-specific BE resources — system is now RCA-agnostic
- Final Investigation model — drop line, drop lot
- Add contract.md — single FE↔BE wire source of truth
- Owner required, Refs for cross-resource, Message gains author + reasoning
- Fe-kickoff.md — first-message brief for the frontend AI agent
- Flip Phase 5-9 endpoints to shipped (✅)
- List BE routes blocking the FE end-to-end UX
- Fix SPA mount section — BE canonical repo is ai-workspace/, not this one
- Mark all v1 polish items shipped
- Add deployment & customization guide (zh-TW)
- Complete documentation set (architecture, dev, user, API)
- Document the _prompt.md per-template appendix convention
- Tell the agent to verify code by running it, not eyeballing
- Document the full Sandbox Protocol contract
- Document the KB chatbot subsystem
- Document the KB subsystem in CLAUDE.md + architecture + contract
- Cover the KB subsystem across the remaining docs
- Docstring every swappable Protocol method + list them as extension points
- Shared turn engine, opaque doc ids, KB streaming/interrupt, offload
- Plan for multi-user collab + KB citation analytics + KB redesign
- Tick P1 done
- Tick P3 done
- Tick P4 done
- Tick P5 done (Size/Updated columns deferred)
- Tick P6 done — all P1-P6 complete
- Add design.md + update kb view reference
- Sandbox-as-single-source-of-truth redesign (P1-P4)
- Tick P1 done
- Tick P2 done
- Tick P3 done (human-reads-snapshot deferred → #13)
- Example uv tool (csv-column-summary) for sandbox provisioning
- Example uv tool (data-fetch) — catalog-based, no LLM-supplied URL
- Data-fetch generates sklearn-augmented datasets (offline, no URL)
- Plan for the open backend issues (#20 #15 #22 #17 #21; #16 parked)
- Mark #20 #15 #22 #17 #21 done in the issue plan
- #21 done — prebuilt-venv copy + template→AgentConfig
- How to add a file preview type (renderers/README.md)
- Mark P1 + P2 complete
- P3 code-QA plan from /grill-me decisions
- Mark P3.0 complete
- Workflows manual, plan, and frontend brief
- Manual + plan (locked via grill-me)
- Add Traditional-Chinese README with per-workflow usage
- Move README into the default profile + make it end-user facing
- Add #113 plan — detect & block LLM repetition loops
- Correct CLAUDE.md `.contains` note for specstar 0.11.9 (#181)
- Plan for HTTP sandbox (#60)
- HTTP sandbox host deploy example + manual (#60 P5)
- Clarify kb_search vs ask_knowledge_base layering (#270)
- Authoring guide, live check, 100% coverage (#298 P6)
- PR2 collection-enforcement handoff (#262 P2)
- PR2 items 1-3,5 done; defer SourceDoc inheritance (#262 P2)
- Document help-content update flow; fix route param type (#230 P6)
- Document job runner / API pod split (#312 follow-up)
- Add contribution log and consolidate developer guide
- Add mkdocs Material site + code-grounded subsystem architecture guide
- Translate English nav docs to Traditional Chinese (zh-TW)
- Follow-up plan — fixes + scale (P1-P8, Q1-Q5 grill-locked)
- Sync evergreen reference docs with features merged since c5ab8de
- Fix broken source-file links failing mkdocs --strict
- Mark P7 complete in plan progress
- P8 — CLAUDE.md shared-dir + isolation conventions, facade docstring
- Mark P1–P6 done + record implementation deviations
- Add "extending the platform" guide (tools/skills/workflows × dev/user)
- Explain where a tool package's command list comes from
- Add the decorator-based tool dispatcher variant
- Show a second command with distinct Args in the decorator example
- Make the skill guide actually produce a working skill
- Spell out how a user workflow.json is promoted to a dev built-in
- Add task-oriented configuration guide + config.example quickstart
- Describe apps uniformly (App = template, no per-app special-casing)
- Document per-item skill tri-state + apply-this-turn
- Plan for AI proactive doc clarification questions
- Document per-sandbox .home / SANDBOX_HOME launcher routing
- Plan for the wiki-update tool + KB-chat 即時指正 UX
- Grill-locked plan for PM app + entity framework
- Record P5–P10 follow-up phases to complete the plan
- Workflow DSL unified-reference-model spec (grill-me locked decisions)
- Add workflow.json visual manual + deep-rewrite workflows.md §22 (post-#428)
- Fix mkdocs --strict build (repoint config.example.yaml links; nav the #428 reference)
- Add the workflow visual manual to the mkdocs nav (was orphaned)
- Grill-me 定案 plan (P1–P9 + follow-up)
- P3 gate-vs-steer guidance + P1/P2 authoring (reads, entity caps)
- P9 — document create_new (live) + send_notification window
- Implementation plan for FE defects batch + #105 quality display
- PM app frontend epic plan (P1 foundation + 6 parallel follow-ups)
- Full-stack collab + live-sync plan (gate / broadcast / feed / presence)
- Document the git-cliff → changelog → release flow


### Fixed

- Expose custom routes in /openapi.json + report.v1.md seed
- Outline for report files, working report buttons, drop stubs
- Drag move-not-copy, draggable folders, short ids, tab chrome
- Pane focus polish — prune empty panes, no border, dim inactive
- Editors fill the pane; drop redundant in-editor path headers
- Prune descendants when acting on a folder-spanning selection
- Live image preview from buffer + edge-to-edge editor padding
- Tidy the search panel layout
- SPA deep-route fallback + persist assistant replies
- Separate reasoning, fix turn bleed, fold logs, stick-to-bottom
- Route reasoning by stream-event type, not just <think>
- Extract tool call_id from dict raw_item (LiteLLM path)
- Don't leak tool-call argument deltas into the answer
- Refresh tree after agent turn; report real max-turns limit
- Decouple system prompt from the template profile
- Make 'python' resolve to python3 inside the jail
- Suggestion chips come only from AgentConfig, no FE fallback
- Serve index.html with Cache-Control: no-cache
- Keep live timestamp on streaming deltas + 24h time format
- Folder upload actually picks a directory
- Dark-mode text + correct logo stroke weight
- Brand lockup proportions + non-wrapping slogan
- Form controls readable in dark mode
- Default the agent to the first config in the store (#2)
- Offload KB ingest store/index off the event loop (#6)
- Slash-free opaque SourceDoc ids (#5)
- Single-source the current user so created_by can't diverge
- Align home stat tiles right of the heading, bottom-aligned
- #9 — readable slash-free doc ids (U+2215), drop full percent-encoding
- Default embed model bge-m3 + log swallowed indexing errors
- Dark-mode doc colors + New collection modal
- Resolve citation markers written with full-width / CJK brackets
- Consistent "Open full view" icon + label for doc table and drawer
- Open the @mention picker upward so it isn't clipped off-screen
- Proper "open external" icon for Open full view (not the upload arrow)
- Stick-to-bottom no longer fights the user scrolling up
- #20 cap read_file output (offset/limit + line/char budgets)
- #15 embedder timeout + retries + batching
- #17 give the agent cross-turn memory (replay history as input)
- Extract prebuilt package with --no-same-owner
- Stream tool stderr live + data-fetch progress
- Show authoritative created_by as Owner + YYYY/MM/DD date
- Preserve newlines in user messages
- Index SourceDoc.collection_id + DocChunk.source_doc_id/collection_id
- SpecstarFileStore uses a deterministic id — cross-pod correct
- Workspace pane resize works again
- Home page overflow (flexbox min-height: 0)
- ResizeDivider horizontal line was invisible
- /exec never 500s — POSIX-real exit codes + always-structured body
- Don't restart a turn after assistant output has streamed
- Full file-state refresh — server flush + every FE cache busted
- Context card — repeated Save on a new card created duplicates (#106)
- Style the multi-chat shell + auto-open a default chat (§3)
- Render the real AgentPanel in each multi-chat tab (§3)
- Let workflow regen nodes overwrite existing files (§12)
- Decision/action content steps — stop routing long content through tool args (#107)
- Kb_search prompt — semantic queries, stop re-asking the same thing
- Offer Claude/GPT presets in the chat model picker (#110)
- Fill the agent panel's column height so the chat scrolls (#109)
- Let the chat panel drag to (near) full width (#108)
- Chunk VLM-produced Markdown on structure, not token windows (#115)
- Stop ask_knowledge_base over-calling on chit-chat
- Feed parsed text, not raw image bytes, to kb_search + doc view (#114)
- Render PDFs in the filesystem viewer, not raw bytes (#117)
- Make collections upload robust on macOS + folder picks
- Surface specstar created_time/created_by on app items
- Coalesce reindex jobs so a doc can't get stuck in "indexing" (#134)
- Hydrate shared RCA chat history (#139)
- Loosen repetition detector so it stops chopping wide tables (#146)
- UX R2-P2 de-jargon term sweep + wording consistency
- Give context-cards left column its own scrollbar (#180)
- Name unnamed chats by creation time, not "New chat" (#190)
- Page documents by immutable created_time so the list stops flickering (#184)
- Move all backend routes under /api so the SPA owns the root
- Strip server metadata from app item PUT body (#201)
- Don't inject a mid-conversation system message after interrupt
- Narrate startup steps + Postgres connect_timeout (#208)
- Recover cross-pod blank app chat (store-poll fallback + sticky regex)
- Bump specstar 0.11.8 -> 0.11.9 to fix PG connection exhaustion (#204)
- Show raw tool name when no i18n label matches (#206)
- Bump specstar to 0.11.9 for exact context-card key lookup (#181)
- Light up the reviewed gate step after a human decision (#176)
- Render SVG previews crisp + higher-res VLM raster (#185)
- Fan out large index jobs to dodge RabbitMQ consumer-ack timeout (#227)
- Context-card keys reflect exact lookup + markdown body (#182, #183)
- Async-ingest uploads into collections + strip uploads/ prefix (#234)
- Collapse index failure list into a default-closed disclosure (#224)
- Show loading skeleton for app items, not a false-empty hero (#225)
- Credit worker-written artifacts to the run's requester (#186)
- Preview .tsv files as a table
- Dark-mode model-picker contrast + font-scale-safe controls
- Make kb_search [n] citation markers clickable in chat (#221)
- Point API reference link at /api/docs respecting base path (#250)
- Whitelist health.judge_llm in the loader's key validator (#231)
- Boundary brackets, rectangular die, vertical labels (#285 review)
- Chunk-based token estimate for collection totals (#88)
- Default quality_judge to retrieval_llm so a rubric alone enables doc scoring (#105)
- Bump specstar 0.11.11 -> 0.11.12 — claim stamps last_heartbeat_at
- Compute the sticky-routing hash key in a server-snippet from $request_uri
- Route unjailed exec `python` to the python-stack carrier
- Restore undone prompt into the composer
- Pin FileTree "Files" header sticky-at-top
- Mirror large agent files into the durable snapshot
- Default app homepage items to updated_time desc
- Route carrier launcher HOME to per-sandbox SANDBOX_HOME, fail-safe default
- Wire per-sandbox .home into the exec path + provisioning
- Populate the auto-card picker's document list
- Keep a strong ref to the background promote task (GC footgun)
- Register CardGenRun/Unit via lazy import to avoid a cycle
- Type-tag wiki card-gen ids so same-path docs stay distinct
- Refresh the Cards view after committing a run from 待審核
- Give PM issues a `span` so the gantt view has data
- Cap and centre app-chat content in a reading column
- Build entity-tool descriptions non-strict too (P5 follow-up)
- Serve the denylist metas-only + close coverage (P5 follow-up)
- Map undefined design tokens to real tokens (P1)
- Flex min-size traps + font-mono nits (P2)
- Give shared <Btn> real hover/disabled via a .btn class (P3)
- Shared <ModalShell> + z-index scale; migrate 9 modals (P4)
- Migrate remaining standard modals onto <ModalShell> (P5)
- Markdown table scroll + tokenized inline-edit controls (P6)
- Route hand-rolled action buttons through .btn (#17 / P7)
- Pm app icon, workspace-toggle clarity, kb reindex confirms
- Remove fake hardcoded git status bar (P1)
- SEARCH panel clips overflow, keeps toggles visible (P2)
- FILES sticky header masks scrolled rows (P3)
- Settings About — link affordance + accurate sign-in copy (P4)
- Collapse WIKI GUIDANCE into a disclosure (P5)
- Permission preview reflects selected visibility (P6)
- #105 quality display — verdict label, expandable rationale, breakdown (P7)
- Trap focus inside ModalShell + restore it to the trigger on close
- Don't show critical/severity UI for Apps with no severity field
- P1 — AppIcon never renders a blank tile; kanban glyph; in-app arrows
- P2 — confirm before deleting a KB conversation (was a bare ✕, no undo)
- P3 — route the Review inbox + Glossary auto-generate through i18n
- P4 — agent-panel header + composer no longer break at the default width
- P5 — keyboard focus ring + legible dimmest-text contrast
- P6 — reveal clipped labels on hover with title= tooltips
- Workspace TopBar reachable at 360 + symmetric sidebar effect (P6)
- P1 — "Set search scope" empty state stops impersonating the primary CTA
- P2 — split .kb-chip into a passive label vs an interactive .kb-chip--btn
- P3 — inline-editable DomainField is a real button, not a role=button span
- P4 — the header health dot reads as an interactive link, not decoration
- P5 — toggles show their on/off direction visually, not only in aria/text
- P6 — destructive controls signal danger instead of hiding as neutral
- P7 — workflows get their own glyph instead of the collections icon
- P8 — clearer create label + consistent pin wording
- P9 — the wiki gets its own book glyph instead of the collections icon
- P10 — consistent create-button pattern for "New card"
- P11 — discoverable pencil to rename a collection (was hidden click-to-edit)
- P12 — a permanent document delete says "Delete", not "Remove"
- P13 — de-jargon the "Re-index" controls to "Re-read" (root cause ③)
- P14 — the "Tune parsing" entry says "Adjust reading" (root cause ③)
- P15 — soften the reading-tuning tool's internal jargon (root cause ③)
- P1 — Diagnostics page shell goes through i18n
- P2 — Diagnostics → Activity tab (telemetry) goes through i18n
- P3 — Model-sanity fitness verdicts go through i18n
- P4 — Model-sanity coverage table goes through i18n
- P5 — Model-sanity question manager goes through i18n
- P6 — friendly copy when a document fails to load (no raw "failed: 404")
- P7 — collection-set picker modal goes through i18n
- P8 — localize two stragglers the P1-P7 audit missed on current master
- P9 — localize KbDocBody's "Cited passage" citation label
- Localize the KB document viewer chrome (Diagnostics-copy follow-up)
- Keep uv.lock in sync in the make release commit


### Performance

- Push dense retrieval into specstar's native vector query
- Count chunks via GROUP BY push-down in documents list (#103)
- List file tree from metadata, never by reading every file
- Push collections dashboard aggregates down (bump specstar 0.11.13)
- Serve the documents list from search metas only (#395 P2)
- Build the OpenAPI schema lazily in create_app
- List pages from meta only, never fetch page blobs (#411)
- List_sources reads paths from meta, not every doc blob (#411)
- Open a doc without the heavy render_document + serial envelope
- Dedup blob fetches + share the doc-list cache (PR-1/3)
- Fetch one item's conversations server-side, not the whole table

