# 更新紀錄 / Release notes

本頁記錄平台面向使用者的重要更新,格式採 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)。
新版本區段由每個合併的 PR(Conventional Commits)自動生成,並隨程式碼一起進版控,所以每次部署
都會是最新的。請勿手改已生成的版本區段——它們會在下次 `make release` 時重新產生。

This page records notable user-facing platform updates in the
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/) format. New version
sections are generated from the merged pull requests (Conventional Commits) and
ship with the code, so the running version is always current. Do not hand-edit
generated sections — they are regenerated on the next `make release`.

---
## [2026.07.23] — 2026-07-23

### Added

- Permission disclosure — surface withheld answers instead of dropping them
- Index-back substring/fuzzy ?qb= queries on ContextCard.norm_keys (kb)
- Per-WorkItem permission control (enter / read / converse / write)
- ITokenService seam for per-user external API tokens (tokens)
- Global collections — a curated baseline the AI always consults
- Surface auto-generate-cards as a settings toggle + move Global into settings
- #534 slice 1 — metric-claim extraction (+ specstar 0.13.0a1) (graph)
- Say when the live stream drops instead of going quiet (chat)
- A metric claim is only as visible as the deck it came from (#534)
- App item 表上直接看得到 access（public / restricted / private） (#578)
- My own chat messages align right, with a real name on them (web)
- 甲 — deterministic naming, stored as versioned state (#534)
- Ask_user — let the agent ask the user a real question, and ship grill-me
- 內建 skill 帶著 scripts / references / 檔案一起走 (#589)
- B — the knowledge graph's entity and relationship layers (#534)
- Query-by-image (placeholder embedder) + context-card attachments UI (#519/#518) (kb)
- ⌘P 檔案面板改真 fuzzy,且輸入 / 能命中 ∕ (web)
- Permission disclosure completion — discoverable restricted, selection-free probe, three-layer switch (#605)
- Cross-pod live SSE streaming via RabbitMQ fanout (event-bus)

### Fixed

- Don't generate cards / build wiki from still-indexing docs — defer to the index hook (kb)
- Seed an App item's files before the WorkItem exists (seed/warm race) (items)
- Make WorkItem access actually changeable — and manageable from both roster surfaces (#306)
- Measure the workspace quota against the live workspace, and enforce it everywhere (#538)
- Unweld the wiki from document search — three sources the KB agent picks between (#537)
- Gate whole operations, and measure from the sweep not the request (#538 follow-up)
- Converge the chat hooks, then stop losing responses and hanging turns
- Agent 看到的 workspace 路徑一律相對，消滅 /foo 誤讀
- Every tool output has a ceiling; list_files reads one directory (agent)
- Stop losing turns — persist, drain and recover instead of dropping them (turns)
- A reload during a live turn no longer looks idle (chat)
- Never let an action produce no reaction at all (chat)
- The sandbox measures itself — quota stops living in app memory (#538)
- Image-to-knowledge 的 term prompt 會被小模型抄成說明文字 (workflow)
- Kb_search 第一次呼叫必失敗 — 補上「不填要放什麼」這句從沒說過的話
- Promote-to-kb 背景任務測試要讓 event loop 活著(而不是加 retry) (test)
- Uploads that worked stop being reported as failures, and exec timeouts stop rebuilding sandboxes
- Wire the poll's failure reporting, and stop hiding a failed Stop (chat)
- Mark the hole, give up on a turn that never began, and answer the last two silent actions (chat)
- One limit, one measurement — and disk_usage really runs du (#538)
- 「全部重新讀取」改成射後不理的 job，並明確回報已送出 (#569)
- The quota measures what the user can see and delete (#538)
- 「重建 wiki」改成射後不理的 job（與 #569 同一題） (#571)
- 給每份卡在「處理中」的文件一個終點 (#573)
- A card proposal is graded against CARDS, not against the wiki (#506)
- A superuser sees the workspace, not an empty shell (web)
- The usage bar goes stale after a turn on host-managed durable (#538)
- One python, one pip — and a login shell that keeps them (sandbox)
- Read_image 每次都失敗——包裝層把工具輸出當成字串 (#584)
- Saving a work item's settings no longer publishes it (web)
- 移動工作區檔案失敗時不再留下兩份，而且會說明原因 (#588)
- 內建 skills / workflow templates 沒被打包進 image (docker)
- Strict tool schema so the model can't guess field names (ask_user)
- Plain exec 的 HOME 改指 per-sandbox .home，不再是 workspace (sandbox)
- Same-pod reconnect replay — in-pod broadcast buffer (?since=) (turns)
- Stop stamp race — cancel reliably in the epoch-stamp window (turns)
- Cross-pod Stop actually stops the run (via the shared turn epoch) (workflow)
- Admin 誤擋全面修正——KB 側 owner-only 閘接上 superuser、absent/unknown permission 語意、身分載入閃爍 (web)

### Performance

- Bound the legacy chunk count to its collection (#103)
- Retriever.search stops loading the whole collection (kb)
- Cap the BM25 corpus, and fix kb.retrieval knobs being silently ignored (kb)

### Documentation

- Document the doc-chunk v6 text-index backfill
- Document sparse_corpus_cap and the two quality knobs
- Plan-issue-520 的「另開 issue」已是 #551,不再是待辦

## [2026.07.16] — 2026-07-16

### Added

- Main agent reads images directly — inline attachments + read_image hands back the raw image (vlm)
- Defect Library — generic KB platform extensions (P2/P6/P7/P8/P9/P10); customization removed (#513)

### Fixed

- Item create never strands the user (backend best-effort seed + FE surfaces errors)

### Performance

- Never load a member's embedding on the review path (#508)
- Stop rebuilding the file tree quadratically on every render (web)

## [2026.07.15] — 2026-07-15

### Added

- Real review-inbox pagination — extract CardProposal resource + native DB paging (#512) (#511)
- Structured logging at key operational nodes (observability)

### Fixed

- Chown app/host-written files to the per-item uid in isolated sandbox (#504)

### Changed

- Generate changelog per-PR (first-parent), retire git-cliff (#441)

### Documentation

- Plan — closed-loop cardgen/doc-question (perf, quality, dedup, editable keys) (#506)

## [2026.07.09] — 2026-07-09

### Added

- Advertise App-declared-but-off tools to the agent (known, not callable) (#480)
- Wiki daily reflection — periodic consolidation (concepts/merge/split) (#479)
- Read_source always shows the full source path (#485)
- Content-addressed chunk dedup — alias identical content, re-home on delete (#104)
- P11 — cold-wake restore progress (還原中 N/M) + idle-TTL ops (#492)

### Fixed

- Global 審核 review inbox — filterable table + drawer, permission-aware (#481)
- Look up the glossary for terms in kb_search passages instead of guessing (#484)
- PM-app entity views — cohesive visual redesign (#448)
- Dedup multi-unit content on the fan-out path + drop orphan chunks in retrieval (#104)
- Bind chunks to content, retire the source_doc_id cascade (remove re-home) (#104)
- Durable sandbox data safety — NFS tree + host-side rsync (#492/#493)
- Card-gen zero-output + public-collection 404 — two silent-swallow bugs + observability (#494)
- Workflow reliability — verified node contracts + authoring overhaul (keep JSON DSL) (#499)
- 區分 sandbox durable filestore 與 API specstar filestore (#501)

## [2026.07.06] — 2026-07-06

### Added

- SpecstarFileStore on per-workspace blob model (filestore)
- Add Workspace / AgentConfig / Conversation Structs (resources)
- Tool wrappers over Sandbox + FileStore (agent)
- FastAPI app factory + SSE message endpoint (api)
- Add LocalProcessSandbox + DockerSandbox adapters (sandbox)
- LitellmAgentRunner — agents SDK + LiteLLM/Ollama (api)
- React SPA chat page + entrypoint + CLAUDE.md docs (web)
- Retry-with-feedback loop for small-model tool-call errors (agent)
- WorkspaceRegistry — sticky per-workspace session state (api)
- FS↔Sandbox bidirectional sync + Files API (sync)
- Interrupt support — DELETE endpoint + cancel-on-new-POST (api)
- Idle-kill lifecycle — background reaper + lifespan teardown (api)
- Refined SSE event variants — ToolCallParseError + MaxTurnsExceeded (api)
- Seed default template on POST /investigation (rca)
- System prompt + default_rca_agent_config (rca)
- Idle 8h default + POST /investigations/{id}/close (api)
- PUT /investigations/{id}/files/{path} for raw byte upload (api)
- Default Dockerfile + jupyter_client/ipykernel deps (sandbox)
- Expose_port Protocol + 3 adapter impls (sandbox)
- KernelService + CellEvent types (kernels)
- Notebook cell execute + interrupt + kernel restart (api)
- Wire close/cancel/interrupt/restart + Close button (web)
- POST /investigations/{id}/exec + MemoryFileStore default (api)
- Finish v1 polish — restart, upload, settings, split, terminal (web)
- DELETE /files/{path} + POST /files/move (api)
- Profile-based seeding + .tpl substitution (templates)
- GET /activity recent-activity feed + event hooks (api)
- Shared per-path file buffers for live split-pane sync (web)
- Monaco editor — syntax highlight, line numbers, ⌘F, minimap (web)
- Resizable + collapsible panels (⌘B / ⌘J) (web)
- ⌘W close tab + ⌘1-9 jump to Nth tab (web)
- VSCode tab behaviors — preview / pin / drag / context menu (web)
- Collapsible file tree + right-click menu (web)
- Working light / dark / system theme (web)
- Template picker in New Investigation modal (web)
- Breadcrumb redesign — topics > product > title + filters (web)
- Notifications popover backed by GET /activity + unread badge (web)
- Multi-pane editor + inline file tree create/rename (web)
- Edge-drop panes, file-tree drag move/copy, sidebar trim
- Real VSCode editor groups + fix file-tree drag highlight (web)
- Search/replace, agent-config routing, pure close (api)
- Agent-config picker + pure-close menu (web)
- VSCode-style global search + replace panel (web)
- Clickable breadcrumb with sibling dropdowns (web)
- Honest directories in the FileStore + endpoints
- Real folders in the file tree (web)
- Explicit-save buffer model (no autosave) (web)
- Imperative confirm Dialog (useDialog) (web)
- Dirty tabs, ⌘S save, save-on-close, middle-click close (web)
- File-tree multi-select + delete-open-file handling (web)
- Replace prompt on move/copy name collision (web)
- Replace prompt on rename + new-file/folder collisions (web)
- Every file is editable, even binary (lossless byte codec) (web)
- Agent quick-prompt suggestions live on AgentConfig (BE)
- Home table — no wrapping, Topics column, reordered columns (web)
- Templates gallery (sidebar Templates → pick → New Investigation) (web)
- Enter-to-send + markdown-rendered agent replies (web)
- Fuller Agent Log + Run History panels (web)
- Terminal no longer freezes on interactive programs
- Live token telemetry (↑/↓ tok/s) + richer run history
- Render LaTeX; fix final token reset; ellipsis truncated args (web)
- Replace default profile skeleton with a single SOP.md (templates)
- Real isolation via user-namespace + chroot jail (sandbox)
- Live tool output streaming in run history
- File-tree refresh, folded+timestamped agent log (web)
- Persist message timestamps + tool name/args so the log survives reload
- Frozen knowledge-base resource model + registration (kb)
- Chunker protocol + FixedTokenChunker (kb)
- Embedder protocol with asymmetric query/doc prefixes (kb)
- Document ingestion (upload → SourceDoc + embedded chunks) (kb)
- Cross-reference markdown link rewriting (pure) (kb)
- Collection/document/render API routes + wire into create_app (kb)
- Rank fusion (RRF) + MMR diversification (kb)
- BM25 sparse keyword scorer (kb)
- Parent-document text-merge of retrieved chunks (kb)
- Hybrid Retriever core (dense + sparse + RRF + MMR + merge) (kb)
- Multi-query expansion (LLM) wired into the Retriever (kb)
- Kb_search agent tool + KB-flavoured AgentToolContext (kb)
- Parse [n] answer markers into Citations (kb)
- Default KB AgentConfig + system prompt (kb)
- KbChat threads + SSE chat endpoint (agentic, with citations) (kb)
- Ask_knowledge_base — RCA agent consults the KB agent (kb)
- KB API client (collections, documents, chats, SSE chat) (web)
- UseKbChat hook — stream a KB turn, refetch for citations (web)
- Ask-agent KB drawer (streaming answer + citation cards) (web)
- KB doc viewer + kb:// link resolver (citation target) (web)
- KB collections management page (create, upload, list docs) (web)
- KB shell — chats history, /kb route, Home entry (web)
- HyDE probe + LLM rerank in the Retriever (kb)
- Persist KB tool-call messages in the chat thread (kb)
- In-text citation highlight + render persisted tool calls (web)
- Real semantic embedding + activate retrieval LLM in the live app (kb)
- Expose document created_by (specstar audit meta) in the list (kb)
- KB agent quick-prompt suggestions + GET /kb/agent (kb)
- KB UX restructure — fast-chat drawer vs full chat views (web)
- Folder upload — ingest a directory like an archive (kb)
- Async ingestion — store fast, embed in the background (kb)
- Filter documents within a collection by name (web)
- Show kb_search results as a distinct panel (not just a chip) (web)
- KB chat matches the RCA agent (reasoning/tools/metrics) (web)
- Make the KB agent cite [n] more reliably (kb)
- Brand entry animation, theme-aware logo, real Settings (web)
- Container image, k8s (base+overlays), GitLab CI, sub-path deploy
- Edit an investigation's title/description/severity/product/topics (web)
- Fetch current user id via api.getCurrentUser (mocked until SSO) (web)
- Add TanStack Query infra + migrate currentUser (web)
- Migrate investigation reads + writes to TanStack Query (web)
- Migrate agentConfigs/templates/activity/fileContent to useQuery (web)
- Migrate KB reads + writes to TanStack Query (web)
- Hydrate agent/KB chat threads via TanStack Query (web)
- Stream the KB agent's intermediate state (#4) (kb)
- KB chat interrupt — wire the server cancel (parity with RCA) (web)
- P1 — identity (UserDirectory) + notifications foundation
- P2 — CitationEvent log + chunk/doc/collection cited counts (kb)
- P3 — read-only chat sharing + shared-with-me (kb)
- P4 — @mention summon (human + agent), decoupled from the agent
- P5 — KB FE redesign (cited surfacing, chunks view, home nav, drawer)
- KB collection list KPI strip (count + most-cited) (web)
- Set root_path on the FastAPI app, not just uvicorn.run (api)
- Document Size + Updated columns (specstar Binary.size + updated_time) (kb)
- P1 — WorkspaceFiles facade + structured file ops + FileEntry.version (sandbox)
- P2 — flip source of truth to the live sandbox (sandbox)
- P3 — durable throttled mirror (version-diff, deletion-aware) (sandbox)
- P4 — edit_file + create-only write_file with compare-and-swap (agent)
- #11 — live LLM/agent telemetry monitor on the SDK trace stream (monitor)
- Add SpecstarMonitor + replace fetch-all-then-filter with indexed queries (monitor)
- #10 — surface the KB retriever's enhancement-LLM thinking in chat (kb)
- Collection card grid matching the handoff + typed responses (kb)
- CollectionPage detail — pickable icon, stats banner, rename/delete (web)
- Aligned doc table + menu-based collection actions + reindex-all (kb)
- Upgrade doc drawer — meta header + action bar (open/download/reindex/remove) (kb)
- Collections landing — rich header, tabs, filters; editable description; scroll (web)
- KB chats — list tabs + rich conversation header (share/pin/export) (web)
- Align Investigations home header to design (web)
- Stick-to-bottom for KB chat + reasoning + tool-log panes (web)
- Keep cumulative token status visible (paused) during tool calls (web)
- Sandbox tool provisioning — install per-tool deps into the sandbox (agent)
- #22 persist final token metrics on the assistant message (BE half) (api)
- #21 wire sandbox tool provisioning into the runner (agent)
- Tool-demo profile to smoke-test provisioned tools with an LLM (templates)
- Export an investigation's full conversation as JSON
- Copy a prebuilt relocatable venv into the sandbox (provision)
- Bind a template profile to its own AgentConfig (templates)
- Wire sample tools into the normal entry; drop the launcher
- Self-contained tool packages that run in the chroot jail (provision)
- LLM base_url + api_key (chat + separate embedder) (settings)
- --plot (distributions + correlation heatmap) (csv-column-summary)
- Mount provisioned tools read-only at /.tools (no per-sandbox copy) (sandbox)
- CSV table preview (web #19)
- Per-message reasoning-effort selector (RCA + KB chat)
- Multi-replica k8s — RWX storage, replicas, per-investigation affinity (#16)
- Extensible preview registry + html/bmp preview types (web)
- Split-pane resize (#30)
- Cross/T handle + linked perpendicular splits (#30)
- T-junction cross handle for split panes (VSCode-style) (#30)
- P1 — LlamaIndex IngestionPipeline for doc ingest (kb)
- P2 — chat → knowledge insight extraction (kb)
- Route code files (.py/.ts/.tsx/.js/.jsx) through CodeSplitter (kb)
- CodeRepoIngestor — clone git remote + ingest tracked files (kb)
- Per-collection embedder + dual-vector DocChunks (D1) (kb)
- Retriever fan-out across embedding + embedding_alt (kb)
- Wire kb_code_embedder + POST /kb/collections/:id/sync (api,factories)
- CodeRepoSweeper + lifespan-driven background sync (kb,api)
- Dispatcher — 3-stage CLI contract helper for tool authors (tooling)
- Prebuild.py — build self-contained tool packages (tooling)
- Registry.py — discover prebuilt bundles + build FunctionTools (tooling)
- Migrate data-fetch to 3-stage JSON contract (sample-tools)
- Migrate csv-column-summary to multi-command package (sample-tools)
- Tool-demo template + end-to-end integration (tooling)
- Skills — host-side progressive disclosure for RCA agent (rca)
- Compose_system_prompt appends Available skills index (rca)
- Read_skill tool + template_profile context plumbing (agent)
- Thread template_profile through runner + app (api)
- Seed methodology template with 3 RCA skills (rca)
- Final §A skill gate + plan check-off (rca,api)
- Topic Hub App + workflows (#100) + context cards (#106) + decide-then-act (topic-hub)
- Wheel-zoom + drag-pan in the image viewer (workspace)
- Detect & gracefully block LLM repetition loops (#113)
- Add read_image tool calling the VLM over workspace images (#112) (agent)
- Update context cards (#111) (topic-hub)
- Collapse the workspace for a full-width chat + fix d… (workspace)
- Legible never-freeze turn-wait status (web)
- Live run activity, no-op reasons, and fix chat export (workflow)
- Collection-set picker UI (#142) (topic-hub)
- Show loading while HTTP requests are in flight (#151) (web)
- Expand truncated sanity-matrix output into a read-only modal (#118) (web)
- #160 de-jargon the UI + introduce a simple i18n layer (web)
- Make KB views URL-addressable (#93) (web)
- Cap kb_search calls per KB-chat turn (#195) (kb)
- Busy-aware LLM failover & switching (#196, #131)
- Per-step status board — alive vs dead per step (#178) (workflow)
- Show context-card diff at the →collections review (#205) (topic-hub)
- One-click re-index of only the failed docs (#223) (kb)
- Single-chat-leaning multichat for every App workspace (#200) (apps)
- Accept file uploads in the run trigger (#197) (workflow)
- HTTP sandbox — client + self-hosted host service (#60) (sandbox)
- Adjustable system font size (#226) (web)
- Workspace-aware + defensive base preamble; rename ls→list_files (#241) (agent)
- Download files & folders from KB and workspace (#247) (downloads)
- Retain page/section provenance so chunks have big-picture context (#254) (kb)
- Profile-configurable upload folder + folder/big-file chat attach (#198) (attach)
- 自動 context card generation (#175) (#268)
- Bundle ppt/excel libs in python-stack; deprecate DockerSandbox (#252) (sandbox)
- Agent knows who it is talking to (#242)
- Structural provenance filter for kb_search (#263) (kb)
- Diagnostics app — coverage table + AI scoring (#231)
- Per-workspace storage quota + blob GC (#245) (filestore)
- Ranked collection pills + shared picker modal for KB chat (#271) (kb)
- Permission model + central authorize() (#262 P1) (perm)
- Make_deck — designed pptx via a multimodal sub-agent loop (#284) (deck)
- Authoring DX — scaffold + check CLI + guide (#287) (workflow)
- Extensible scientific plotting catalog + VLM self-review (#285) (sci-plot)
- Lookup_user tool — resolve a teammate by handle (#275) (agent)
- Score document quality + down-weight low-quality docs in search (#105) (kb)
- User+AI co-create a loadable skill (#298) (skills)
- Tiered, web-editable collection scope for RCA (#280) (kb)
- Collection access enforcement (#262 P2) (perm)
- Platform Help page — usage guides + release notes + AI Q&A (#230) (help)
- Per-item web tool picker + backend tool catalog (#322) (tools)
- Block encrypted/unreadable uploads with pluggable checks (#325) (kb)
- Fix sync→build trigger + job-queue fan-out + incremental code-wiki (#281 follow-up)
- Configurable, resilient failover so a busy system doesn't kill the turn (#196) (failover)
- Findability probe modal — interactive prompt-tuning playground (#328)
- Web form to ingest a codebase + async code_sync job + daily auto-sync (#355)
- Tune parsing — per-doc prompt escape hatch + answer preview (#356)
- Cross-pod turn cancellation + restore sub-path sticky routing (#349)
- Teach workspace agents how users upload files (#363)
- Paste + drag-drop file/image upload UX (#364)
- Structured-data viewers for json/jsonl/csv/tsv/yaml (#361)
- KB chat titles — name_hint labels + inline rename (#357)
- Launch a workflow in the current chat (takeover) (#343)
- Per-item skill picker — list · tri-state toggle · apply-this-turn (#380)
- AI asks about unclear terms instead of hallucinating (#377)
- Cross-path index-result cache (#390) (kb)
- Doc filetree filter + resizable tree width (#405) (#402)
- Tool + KB-chat flow to correct the wiki (#397)
- Instrument durable mirror/restore/gc telemetry + /monitor/summary (#407)
- Parallelise card generation via per-doc fan-out (#414)
- Card-gen source picker + persistent 待審核 review inbox (#415)
- SourceDoc read access inherits its collection's visibility (#303)
- App WorkItem resource-layer permission enforcement (#306)
- Agent-tool authorization — AI bounded by ceiling ∩ speaker (#309)
- KbChat resource-layer permission enforcement (#304)
- Ask_knowledge_base transitive read_content check (#305)
- Logical Group entity (minimal viable) (#307)
- Collection sharing UI — generic role-based permission dialog (#310)
- Workflow DSL — unified references, outputs, switch, over, fan-in, gate revise (#428)
- Per-doc read override (intersect-tightens collection inheritance) (#308)
- Non-idempotent capability dedup framework (#435)
- Workflow engine gaps — reads-cache, entity write, time+event triggers (P1–P9) (#429)
- P7/P8 — un-gate create_new + send_notification per-window (build the per-invocation boundary #429 never shipped) (#435)
- P10+P11 — close the event-dispatch seams (agent-write origin + D2d backfill) (#444) (#429)
- PM app 前端 P1 — 基建 + 核心可操作 (table/寫入/容錯) (#448)
- Table 多選 + 批次 status/actor(扇出 N 筆 update) (#452)
- PM gantt 互動 — 拖曳改起訖 / 縮放 / 泳道 (#450)
- PM health view — diagnostics filter + jump-to-record (stacked on #457) (#454)
- PM board renderer interactions — drag-to-move + card face + out-of-vocab columns (#463) (#451)
- PM 前端 C2 — 單一 entity 檔案編輯器(frontmatter 表單/YAML + body) (#453)
- PM collab — read-only gate + live-sync + activity feed + presence (#455)
- Whole-app responsive layout — shells collapse cleanly below 768px (#464)
- Release notes on GitHub + the web (git-cliff + CalVer) (#441)

### Fixed

- Expose custom routes in /openapi.json + report.v1.md seed (api)
- Outline for report files, working report buttons, drop stubs (web)
- Drag move-not-copy, draggable folders, short ids, tab chrome (web)
- Pane focus polish — prune empty panes, no border, dim inactive (web)
- Editors fill the pane; drop redundant in-editor path headers (web)
- Prune descendants when acting on a folder-spanning selection (web)
- Live image preview from buffer + edge-to-edge editor padding (web)
- Tidy the search panel layout (web)
- SPA deep-route fallback + persist assistant replies (api)
- Separate reasoning, fix turn bleed, fold logs, stick-to-bottom (web+api)
- Route reasoning by stream-event type, not just <think> (api)
- Extract tool call_id from dict raw_item (LiteLLM path) (api)
- Don't leak tool-call argument deltas into the answer (api)
- Refresh tree after agent turn; report real max-turns limit
- Decouple system prompt from the template profile (prompt)
- Make 'python' resolve to python3 inside the jail (sandbox)
- Suggestion chips come only from AgentConfig, no FE fallback (web)
- Serve index.html with Cache-Control: no-cache (api)
- Keep live timestamp on streaming deltas + 24h time format (web)
- Folder upload actually picks a directory (web)
- Dark-mode text + correct logo stroke weight (web)
- Brand lockup proportions + non-wrapping slogan (web)
- Form controls readable in dark mode (web)
- Default the agent to the first config in the store (#2) (api)
- Offload KB ingest store/index off the event loop (#6) (api)
- Slash-free opaque SourceDoc ids (#5) (kb)
- Single-source the current user so created_by can't diverge (api)
- Align home stat tiles right of the heading, bottom-aligned (web)
- #9 — readable slash-free doc ids (U+2215), drop full percent-encoding (kb)
- Default embed model bge-m3 + log swallowed indexing errors (kb)
- Dark-mode doc colors + New collection modal (web)
- Resolve citation markers written with full-width / CJK brackets (kb)
- Consistent "Open full view" icon + label for doc table and drawer (web)
- Open the @mention picker upward so it isn't clipped off-screen (web)
- Proper "open external" icon for Open full view (not the upload arrow) (web)
- Stick-to-bottom no longer fights the user scrolling up (web)
- #20 cap read_file output (offset/limit + line/char budgets) (agent)
- #15 embedder timeout + retries + batching (kb)
- #17 give the agent cross-turn memory (replay history as input) (agent)
- Extract prebuilt package with --no-same-owner (provision)
- Stream tool stderr live + data-fetch progress (#23)
- Show authoritative created_by as Owner + YYYY/MM/DD date (web)
- Preserve newlines in user messages (web #18)
- Index SourceDoc.collection_id + DocChunk.source_doc_id/collection_id (#14)
- SpecstarFileStore uses a deterministic id — cross-pod correct (#16)
- Workspace pane resize works again (#30)
- Home page overflow (flexbox min-height: 0) (web)
- ResizeDivider horizontal line was invisible (web)
- /exec never 500s — POSIX-real exit codes + always-structured body (#27)
- Don't restart a turn after assistant output has streamed (#26)
- Full file-state refresh — server flush + every FE cache busted (#31)
- Chat layout #108/#109/#110 + VLM markdown & table chunking #115/#116 (workspace, kb)
- Stop ask_knowledge_base over-calling on chit-chat (agent)
- Feed parsed text, not raw image bytes, to kb_search + doc view (#114) (kb)
- Render PDFs in the filesystem viewer, not raw bytes (#117) (kb)
- Make collections upload robust on macOS + folder picks (kb)
- Workspace footer shows "Opened NaN/NaN/NaN" (#137)
- Coalesce reindex jobs so a doc can't get stuck in "indexing" (#134) (kb)
- Hydrate shared RCA chat history (#139) (web)
- Loosen repetition detector so it stops chopping wide tables (#146) (agent)
- UX R2-P2 de-jargon term sweep + wording consistency (#171)
- Give context-cards left column its own scrollbar (#180) (kb)
- Name unnamed chats by creation time, not "New chat" (#190) (kb)
- Stop documents list flickering during indexing (#184) (kb)
- Move all backend routes under /api so the SPA owns the root (#177)
- Strip server metadata from app item PUT body (#201) (web)
- Don't inject a mid-conversation system message after interrupt (#199)
- Narrate startup steps + Postgres connect_timeout (#208) (boot)
- Recover cross-pod blank app chat (store-poll fallback + sticky regex) (#202)
- Bump specstar 0.11.8 → 0.11.9 to fix PG connection exhaustion (#204) (deps)
- Show raw tool name when no i18n label matches (#206) (web)
- Exact context-card key lookup on Postgres via specstar 0.11.9 (#181) (kb)
- Light up the reviewed gate step after a human decision (#176) (workflow)
- Render SVG previews crisp + higher-res VLM raster (#185) (kb)
- Fan out large index jobs to dodge RabbitMQ 406 consumer-ack timeout (#227) (kb)
- Async-ingest uploads into collections + strip uploads/ prefix (#234) (topic-hub)
- Context-card keys reflect exact lookup + markdown body (#182, #183) (topic-hub)
- Collapse index failure list into a default-closed disclosure (#224) (kb)
- Loading skeleton for app items, not a false-empty hero (#225) (apps)
- Credit worker-written job artifacts to the run's requester (#186) (kb)
- TSV preview, dark-mode model-picker contrast, font-scale-safe controls (#246 #255 #256) (ui)
- Retry transient errors on index jobs (BadGateway 502, …) (#249)
- Monotonic fan-out indexing progress bar (#248)
- Make kb_search [n] citation markers clickable in chat (#221) (kb)
- API reference link respects /api + base path (#250) (web)
- Chunk-based token estimate for collection totals (#88) (kb)
- Boundary brackets, rectangular die, top-to-bottom vertical labels (#285 review) (sci-plot)
- Default quality_judge to retrieval_llm so a rubric alone enables doc scoring (#105) (kb)
- Restore rich run progress in workflow chats (#331) (web)
- Bump specstar to 0.11.12 (claim stamps heartbeat → no stale-job false positive) (#342)
- Shared per-item sandbox dir + per-item isolation (multipod data loss) (#345)
- Route unjailed exec `python` to the python-stack carrier (#350)
- Restore undone prompt into the composer (#370)
- Multi-pod http sandbox coherence — share the address, not the folder (#366)
- Pin FileTree "Files" header sticky-at-top (#346)
- Mirror large agent files into the durable snapshot (drop 10MB cap) (#374)
- Default app homepage items to updated_time desc (#383)
- Per-sandbox HOME for the carrier launcher (no shared /tmp package leak) (#393)
- Let users generate + run workflows in playground/rca (opt-in author-workflow) (#400)
- Populate the auto-card picker's document list (#394)
- Parse code files into the KB instead of feeding them as raw text (#389)
- Cap and centre app-chat content in a reading column (chat)
- Design-token integrity, layout, shared Btn + ModalShell, button sweep (P1-P7) (#445)
- Pm app icon + workspace-toggle clarity + kb reindex confirms (ux)
- FE defects batch (screenshot review) + #105 quality-score display (#460)
- ModalShell focus trap + hide critical UI for no-severity Apps (#467)
- FE UX audit fixes — P1–P6 (blank icons, delete-confirm, i18n, layout, a11y, tooltips) (#456)
- Control affordance — passive-looking controls, icon overloading, weak state/danger signals (#466)
- Localize Diagnostics / Model-sanity + kill leaked internal strings (#465)
- Localize the KB document viewer chrome (#456)

### Performance

- Push dense retrieval into specstar's native vector query (kb)
- Count chunks via GROUP BY push-down in documents list (#103) (kb)
- List file tree from metadata, never by reading every file (#362)
- Push collections dashboard aggregates down (specstar 0.11.13) (kb)
- Metas-only documents list + cheap status poll (#395) (kb)
- Cut backend CI from ~75min via lazy OpenAPI + sharded test matrix (ci)
- Metas-only page listing, no per-page blob fetch (#411) (wiki)
- Open a doc without the heavy render_document + serial envelope (kb)
- Dedup blob fetches + share the doc-list cache (content-read audit PR-1/3) (kb-ide)
- Fetch one item's conversations server-side, not the whole table (content-read audit PR-3/3) (api)

### Changed

- Rename Workspace → Investigation + RCA pivot model (resources)
- Composition-root factories (Settings + get_*(settings) -> Protocol)
- Shared ChatTurnEngine for RCA + KB chat turns (api)
- Workspace is ~ (a subdir); tools/caches live outside it (sandbox)
- Unify Home + KbHome "Ask agent" on AskAgentLauncher (web)
- ResizeDivider anchors on drag start (fixes drift) (web)
- Replace ToolDef with PackageInfo throughout (agent,tooling,rca)
- Consolidate cross-app sandbox + security guidance into shared preambles (prompts)
- One react-query content cache under the file buffer (content-read audit PR-2/3) (fe)
- Migrate WorkspaceShell edit-item modal to <ModalShell> (#445)

### Documentation

- Add README with Ollama setup walkthrough
- Full implementation plan (what's done + what's left)
- Split implementation plan into backend + frontend
- Pivot plans to RCA 3.0 vertical app
- Collapse RCA-specific BE resources — system is now RCA-agnostic
- Final Investigation model — drop line, drop lot (plan-backend)
- Add contract.md — single FE↔BE wire source of truth
- Owner required, Refs for cross-resource, Message gains author + reasoning (contract)
- Fe-kickoff.md — first-message brief for the frontend AI agent
- Flip Phase 5-9 endpoints to shipped (✅) (contract)
- Mark all v1 polish items shipped (fe-gaps)
- Add deployment & customization guide (zh-TW)
- Complete documentation set (architecture, dev, user, API)
- Document the _prompt.md per-template appendix convention
- Tell the agent to verify code by running it, not eyeballing (prompt)
- Document the full Sandbox Protocol contract (sandbox)
- Document the KB chatbot subsystem (readme)
- Document the KB subsystem in CLAUDE.md + architecture + contract
- Cover the KB subsystem across the remaining docs
- Docstring every swappable Protocol method + list them as extension points
- Shared turn engine, opaque doc ids, KB streaming/interrupt, offload
- Plan for multi-user collab + KB citation analytics + KB redesign
- Tick P1 done (plan)
- Tick P3 done (plan)
- Tick P4 done (plan)
- Tick P5 done (Size/Updated columns deferred) (plan)
- Tick P6 done — all P1-P6 complete (plan)
- Add design.md + update kb view reference (design)
- Sandbox-as-single-source-of-truth redesign (P1-P4) (plan)
- Tick P1 done (plan)
- Tick P2 done (plan)
- Tick P3 done (human-reads-snapshot deferred → #13) (plan)
- Example uv tool (csv-column-summary) for sandbox provisioning (tools)
- Example uv tool (data-fetch) — catalog-based, no LLM-supplied URL (tools)
- Data-fetch generates sklearn-augmented datasets (offline, no URL) (tools)
- Plan for the open backend issues (#20 #15 #22 #17 #21; #16 parked)
- Mark #20 #15 #22 #17 #21 done in the issue plan
- #21 done — prebuilt-venv copy + template→AgentConfig (plan-issues)
- How to add a file preview type (renderers/README.md) (web)
- Mark P1 + P2 complete (plan-llamaindex)
- P3 code-QA plan from /grill-me decisions (plan-code-qa)
- Mark P3.0 complete (plan-code-qa)
- Clarify kb_search vs ask_knowledge_base layering (#270)
- 補 job runner / API 切分文件 (#312 follow-up)
- Add contribution log and consolidate developer guide
- Mkdocs Material site + code-grounded subsystem architecture guide (GitHub Pages)
- Translate English nav docs to Traditional Chinese (zh-TW)
- Sync evergreen reference docs with features merged since c5ab8de
- Fix broken source-file links failing mkdocs --strict
- Add "extending the platform" guide (tools/skills/workflows × dev/user)
- Explain where a tool package's command list comes from
- Add the decorator-based tool dispatcher variant
- Show a second command with distinct Args in the decorator example
- Make the skill guide actually produce a working skill
- Spell out how a user workflow.json is promoted to a dev built-in
- Add task-oriented configuration guide + config.example quickstart
- Add workflow.json visual manual + deep-rewrite workflows.md §22 (post-#428)
- Fix mkdocs --strict build (repoint config.example.yaml links; nav the #428 reference)
- Add the workflow visual manual to the mkdocs nav (was orphaned)
