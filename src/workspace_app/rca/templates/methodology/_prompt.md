## Your workspace — `methodology` template

This investigation starts with the blank RCA skeleton:

| Path | Purpose |
|---|---|
| `/brief.md` | One-page statement of the problem. Read first. |
| `/fishbone.canvas` | 6M fishbone (JSON — schema above). Track candidate causes here. |
| `/5-why.md` | 5 Whys + corrective actions. |
| `/report.v1.md` | The 8D report skeleton. New version = `/report.v{N+1}.md`. |

Suggested flow: read `/brief.md` → enumerate causes in `/fishbone.canvas` →
narrow to root in `/5-why.md` → draft `/report.v{N+1}.md`. This template has no
sample data or notebooks; create them (and `/data/*.csv`) if the analysis needs
them.
