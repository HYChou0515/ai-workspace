"""File-first structured entities (#419).

An *entity* is a workspace file with parsed frontmatter (`issues/5.md`) that a
declared schema projects into views. This package is the opt-in framework: an
App with no `.entity/<type>/` schema behaves exactly as before.
"""
