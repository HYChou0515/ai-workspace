// Auto-generated by AutoCRUD Web Generator
import { createFileRoute } from '@tanstack/react-router';
import { Container } from '@mantine/core';
import { ResourceTable } from '../../../autocrud/lib/components/ResourceTable';
import { getResource } from '../../../autocrud/lib/resources';

export const Route = createFileRoute('/autocrud-admin/telemetry-event/')({
  component: ListPage,
});

function ListPage() {
  const config = getResource('telemetry-event')!;
  return (
    <Container size="xl" py="xl">
      <ResourceTable config={config} basePath="/autocrud-admin/telemetry-event" />
    </Container>
  );
}
