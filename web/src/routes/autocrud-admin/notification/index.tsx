// Auto-generated by AutoCRUD Web Generator
import { createFileRoute } from '@tanstack/react-router';
import { Container } from '@mantine/core';
import { ResourceTable } from '../../../autocrud/lib/components/ResourceTable';
import { getResource } from '../../../autocrud/lib/resources';

export const Route = createFileRoute('/autocrud-admin/notification/')({
  component: ListPage,
});

function ListPage() {
  const config = getResource('notification')!;
  return (
    <Container size="xl" py="xl">
      <ResourceTable config={config} basePath="/autocrud-admin/notification" />
    </Container>
  );
}
