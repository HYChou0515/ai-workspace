// Auto-generated by AutoCRUD Web Generator
import { client } from '../../lib/client';

export type OnDuplicate = 'overwrite' | 'skip' | 'raise_error';

export interface ImportResult {
  loaded: number;
  skipped: number;
  total: number;
}

export interface GlobalImportResult {
  [model: string]: ImportResult;
}

export const backupApi = {
  /** Download full backup (.acbak) for all models — uses the AutoCRUD global /_backup endpoint */
  exportAll: (models?: string[]) =>
    client.get('/_backup/export', {
      params: models ? { models } : undefined,
      responseType: 'blob',
    }),

  /** Upload .acbak archive (global import) — uses the AutoCRUD global /_backup endpoint */
  importAll: (file: File, onDuplicate: OnDuplicate = 'overwrite') => {
    const form = new FormData();
    form.append('file', file);
    return client.post<GlobalImportResult>('/_backup/import', form, {
      headers: { 'Content-Type': 'multipart/form-data' },
      params: { on_duplicate: onDuplicate },
    });
  },

  /** Export Investigation data as .acbak archive */
  exportInvestigation: (params?: Record<string, unknown>) =>
    client.get(`/investigation/export`, { params, responseType: 'blob' }),

  /** Import .acbak archive into Investigation */
  importInvestigation: (file: File, onDuplicate: OnDuplicate = 'overwrite') => {
    const form = new FormData();
    form.append('file', file);
    return client.post<ImportResult>(`/investigation/import`, form, {
      headers: { 'Content-Type': 'multipart/form-data' },
      params: { on_duplicate: onDuplicate },
    });
  },

  /** Export Rca Investigation data as .acbak archive */
  exportRcaInvestigation: (params?: Record<string, unknown>) =>
    client.get(`/rca-investigation/export`, { params, responseType: 'blob' }),

  /** Import .acbak archive into Rca Investigation */
  importRcaInvestigation: (file: File, onDuplicate: OnDuplicate = 'overwrite') => {
    const form = new FormData();
    form.append('file', file);
    return client.post<ImportResult>(`/rca-investigation/import`, form, {
      headers: { 'Content-Type': 'multipart/form-data' },
      params: { on_duplicate: onDuplicate },
    });
  },

  /** Export Conversation data as .acbak archive */
  exportConversation: (params?: Record<string, unknown>) =>
    client.get(`/conversation/export`, { params, responseType: 'blob' }),

  /** Import .acbak archive into Conversation */
  importConversation: (file: File, onDuplicate: OnDuplicate = 'overwrite') => {
    const form = new FormData();
    form.append('file', file);
    return client.post<ImportResult>(`/conversation/import`, form, {
      headers: { 'Content-Type': 'multipart/form-data' },
      params: { on_duplicate: onDuplicate },
    });
  },

  /** Export Collection data as .acbak archive */
  exportCollection: (params?: Record<string, unknown>) =>
    client.get(`/collection/export`, { params, responseType: 'blob' }),

  /** Import .acbak archive into Collection */
  importCollection: (file: File, onDuplicate: OnDuplicate = 'overwrite') => {
    const form = new FormData();
    form.append('file', file);
    return client.post<ImportResult>(`/collection/import`, form, {
      headers: { 'Content-Type': 'multipart/form-data' },
      params: { on_duplicate: onDuplicate },
    });
  },

  /** Export Source Doc data as .acbak archive */
  exportSourceDoc: (params?: Record<string, unknown>) =>
    client.get(`/source-doc/export`, { params, responseType: 'blob' }),

  /** Import .acbak archive into Source Doc */
  importSourceDoc: (file: File, onDuplicate: OnDuplicate = 'overwrite') => {
    const form = new FormData();
    form.append('file', file);
    return client.post<ImportResult>(`/source-doc/import`, form, {
      headers: { 'Content-Type': 'multipart/form-data' },
      params: { on_duplicate: onDuplicate },
    });
  },

  /** Export Doc Chunk data as .acbak archive */
  exportDocChunk: (params?: Record<string, unknown>) =>
    client.get(`/doc-chunk/export`, { params, responseType: 'blob' }),

  /** Import .acbak archive into Doc Chunk */
  importDocChunk: (file: File, onDuplicate: OnDuplicate = 'overwrite') => {
    const form = new FormData();
    form.append('file', file);
    return client.post<ImportResult>(`/doc-chunk/import`, form, {
      headers: { 'Content-Type': 'multipart/form-data' },
      params: { on_duplicate: onDuplicate },
    });
  },

  /** Export Wiki Page data as .acbak archive */
  exportWikiPage: (params?: Record<string, unknown>) =>
    client.get(`/wiki-page/export`, { params, responseType: 'blob' }),

  /** Import .acbak archive into Wiki Page */
  importWikiPage: (file: File, onDuplicate: OnDuplicate = 'overwrite') => {
    const form = new FormData();
    form.append('file', file);
    return client.post<ImportResult>(`/wiki-page/import`, form, {
      headers: { 'Content-Type': 'multipart/form-data' },
      params: { on_duplicate: onDuplicate },
    });
  },

  /** Export Wiki Build State data as .acbak archive */
  exportWikiBuildState: (params?: Record<string, unknown>) =>
    client.get(`/wiki-build-state/export`, { params, responseType: 'blob' }),

  /** Import .acbak archive into Wiki Build State */
  importWikiBuildState: (file: File, onDuplicate: OnDuplicate = 'overwrite') => {
    const form = new FormData();
    form.append('file', file);
    return client.post<ImportResult>(`/wiki-build-state/import`, form, {
      headers: { 'Content-Type': 'multipart/form-data' },
      params: { on_duplicate: onDuplicate },
    });
  },

  /** Export Kb Chat data as .acbak archive */
  exportKbChat: (params?: Record<string, unknown>) =>
    client.get(`/kb-chat/export`, { params, responseType: 'blob' }),

  /** Import .acbak archive into Kb Chat */
  importKbChat: (file: File, onDuplicate: OnDuplicate = 'overwrite') => {
    const form = new FormData();
    form.append('file', file);
    return client.post<ImportResult>(`/kb-chat/import`, form, {
      headers: { 'Content-Type': 'multipart/form-data' },
      params: { on_duplicate: onDuplicate },
    });
  },

  /** Export Notification data as .acbak archive */
  exportNotification: (params?: Record<string, unknown>) =>
    client.get(`/notification/export`, { params, responseType: 'blob' }),

  /** Import .acbak archive into Notification */
  importNotification: (file: File, onDuplicate: OnDuplicate = 'overwrite') => {
    const form = new FormData();
    form.append('file', file);
    return client.post<ImportResult>(`/notification/import`, form, {
      headers: { 'Content-Type': 'multipart/form-data' },
      params: { on_duplicate: onDuplicate },
    });
  },

  /** Export Citation Event data as .acbak archive */
  exportCitationEvent: (params?: Record<string, unknown>) =>
    client.get(`/citation-event/export`, { params, responseType: 'blob' }),

  /** Import .acbak archive into Citation Event */
  importCitationEvent: (file: File, onDuplicate: OnDuplicate = 'overwrite') => {
    const form = new FormData();
    form.append('file', file);
    return client.post<ImportResult>(`/citation-event/import`, form, {
      headers: { 'Content-Type': 'multipart/form-data' },
      params: { on_duplicate: onDuplicate },
    });
  },

  /** Export Check Run data as .acbak archive */
  exportCheckRun: (params?: Record<string, unknown>) =>
    client.get(`/check-run/export`, { params, responseType: 'blob' }),

  /** Import .acbak archive into Check Run */
  importCheckRun: (file: File, onDuplicate: OnDuplicate = 'overwrite') => {
    const form = new FormData();
    form.append('file', file);
    return client.post<ImportResult>(`/check-run/import`, form, {
      headers: { 'Content-Type': 'multipart/form-data' },
      params: { on_duplicate: onDuplicate },
    });
  },

  /** Export Sanity Result data as .acbak archive */
  exportSanityResult: (params?: Record<string, unknown>) =>
    client.get(`/sanity-result/export`, { params, responseType: 'blob' }),

  /** Import .acbak archive into Sanity Result */
  importSanityResult: (file: File, onDuplicate: OnDuplicate = 'overwrite') => {
    const form = new FormData();
    form.append('file', file);
    return client.post<ImportResult>(`/sanity-result/import`, form, {
      headers: { 'Content-Type': 'multipart/form-data' },
      params: { on_duplicate: onDuplicate },
    });
  },

  /** Export Telemetry Event data as .acbak archive */
  exportTelemetryEvent: (params?: Record<string, unknown>) =>
    client.get(`/telemetry-event/export`, { params, responseType: 'blob' }),

  /** Import .acbak archive into Telemetry Event */
  importTelemetryEvent: (file: File, onDuplicate: OnDuplicate = 'overwrite') => {
    const form = new FormData();
    form.append('file', file);
    return client.post<ImportResult>(`/telemetry-event/import`, form, {
      headers: { 'Content-Type': 'multipart/form-data' },
      params: { on_duplicate: onDuplicate },
    });
  },
};
