# 知識圖譜的回答面 — Plan(#534 → #628 / #630 / #633)

> 這份計畫把三個相連的 slice 收在一起:**讓知識圖譜真的能回答問題**。
> #534 建好了圖的骨架(mention / entity / link / claim),但骨架不會回答問題;
> 這裡處理的是「使用者問一句話,圖怎麼變成答案」。
>
> **這是可勾選的追蹤文件** — 每完成一階段就把 `- [ ]` 打勾並 commit。
> 決策脈絡在對應 issue 的討論串,這裡記「要做什麼、為什麼、順序、進度」。

---

## 0 · 進度總覽

| Slice | 內容 | Issue | PR | 狀態 |
| --- | --- | --- | --- | --- |
| 一 | 數字掛上 entity + `lookup_entity` 檔案卡工具 | [#628](https://github.com/HYChou0515/ai-workspace/issues/628) | #629 | P1–P7 完成(draft) |
| 二 | 抽取端統一「誰·什麼屬性·值」,不用型別當分界 | [#630](https://github.com/HYChou0515/ai-workspace/issues/630) | #631 | **P1–P7 完成**(draft) |
| 三 | 名字對上就自動注入檔案卡(不再問模型要不要查) | [#633](https://github.com/HYChou0515/ai-workspace/issues/633) | — | **P1–P6 完成** |
| 四 | 總表:能搜尋、能篩選、能翻頁地巡檢圖裡有什麼 | [#636](https://github.com/HYChou0515/ai-workspace/issues/636) | — | **P1–P5 完成** |

---

## 1 · 為什麼要有這個

使用者定調(2026-07-23/24),圖存在的理由是回答兩類問題:

1. **定義 / 設定題** — 「n5 tv0 por recipe 是什麼」
   期待的答案**不是字典定義**,而是一張檔案卡:名稱、參數、當時的 quality
   review 報告、對應的 slide。
2. **因果題 + 前例** — 「n3 tv1 ox broken 的原因是?有沒有過去經驗可以對照?」
   前半是指向這個失效的因果邊(附原句);後半是同類事件在別的 node / 時期的
   紀錄。

語料特性:**大量簡報,裡面是表格、圖示、箭頭**。這正是向量檢索最弱的地方 ——
答案散在很多張投影片上,要靠「拼」而不是「找一段話」。

> ⚠️ **內部術語極多,任何行話都不准進 prompt**。使用者給的例句只是形狀示範,
> 機制必須通用,不得對範例 overfit。

### 答案的形狀:檔案卡

兩類問題共用同一種答案 — **把一個東西的檔案卡拼出來,每一行附出處**:

```
名稱(含別名) · 類別
文件怎麼說它     良率 = 98.7% (Q3)   [deck-A]
                 POR recipe = PPOOIXUX  [deck-A]
誰用了它         回焊爐 · POR recipe   [deck-A]
它和誰有關       回焊爐 —造成→ 空洞「回焊爐溫度過高造成空洞」[deck-A]
哪些文件提到它   deck-A / deck-B / deck-R
```

---

## 2 · 業界調研(四路平行,2026-07-24)

完整對照表與出處見 [#628 的調研留言](https://github.com/HYChou0515/ai-workspace/issues/628#issuecomment-5064004061)。結論三句話:

- **沒有任何一家用「數字 vs 文字」當分界。** 一家都沒有。LangChain 甚至強制
  `value: str`。我們原本是唯一一個。
- **真正的分界是「你要不要談論它 / 跨它查詢」。** RDF 是硬法則(literal 不能
  當主語);Neo4j 官方原話:「要按日期範圍查,日期就不該是屬性而該是節點」。
- **抽取當下無法預知,業界靠「兩形式並存」**:literal 是節點的「尚未解析」形式
  (Wikidata `P50`/`P2093`、schema.org 容忍 Text)。

| 系統 | 值會變節點嗎 | 誰決定 · 何時 | 分數字/文字? | 矛盾處理 |
| --- | --- | --- | --- | --- |
| cognee | 一定會(結構上沒有屬性可放) | 寫死在 code | 否 | 無(寫入時) |
| LlamaIndex | 預設會(`1982` 變成節點) | 使用者 schema | 否 | 無 |
| Neo4j / LangChain | 屬性預設關閉 → 實際上會 | 使用者 config | 否 | 無(靜默丟棄) |
| GraphRAG | 不會,但烤進散文描述 | LLM,限型別白名單 | 否 | 無(claim 預設關閉) |
| Wikidata | 每個屬性事先宣告、永久不可改 | 人,建屬性時 | 否 | rank + references |
| RDF / Neo4j 指南 | 依「要不要談論 / 跨它查」 | 建模者,可重構 | 否 | 保留競爭陳述 |

---

## 3 · 已定案的決策

- [x] **矛盾偵測不做。** 系統不該替使用者判斷誰對誰錯;需求未明朗,做了吃力
      不討好。頁面只忠實列出「哪份文件說了什麼」,判斷留給讀者。
      (LLM 系四家也沒有一家在寫入時做。)
      ⚠️ 未來若要重開,先有明確需求;別把「這裡有事、自己去查」的告示牌加回來。
- [x] **分界不是型別,是「值 vs 東西」** — 而且**不預先判斷**。一律記
      「誰·什麼屬性·值(verbatim)」;值有沒有自己的節點,由「有沒有文件把它
      當主體談過」決定(= 既有 entity 規則,無 mention 即不存在)。
- [x] **qualifier 分兩層**(Wikidata):`unit` 屬於**值本身**;`period` 屬於
      **這句陳述**。
- [x] **有主語就用主語綁定,共現猜測整段刪除。** 兩套規則並存必有一套是假的。
- [x] **值一律 verbatim**,不解析成數字。顯示時單位只出現一次
      (值已含單位就不再補)。
- [x] **不做屬性白名單**(同 kind 的理由:預先寫死的清單只會往昂貴的方向錯)。

---

## 4 · Slice 一 — 數字掛上 entity + 檔案卡工具(#628 / PR #629)

- [x] **P1** `entity_page` 回傳共現 claims(後來被 slice 二的主語綁定取代)
- [x] **P2** `GET /kb/graph/entities/{id}` 回傳 `claims`
- [x] **P3** entity 頁「文件怎麼說它」區塊
- [x] **P4** `lookup_entity` — 確定性、零 LLM 的檔案卡工具(同
      `lookup_glossary` 家族,不受 #537 搜尋配額裁工具集)
- [x] **P5** kb_chat presets 授權 + prompt 列為第四種來源
- [x] **P6** 驗收:真模型跑「回焊爐是什麼?」→ 檔案卡作答
- [x] **P7** 移除矛盾偵測(使用者拍板)

---

## 5 · Slice 二 — 抽取端統一(#630 / PR #631)

**問題**:`extract.py` 的 prompt 第一句寫死「Extract every metric that carries a
**numeric value**」,所以「X 的 recipe 是 PPOOIXUX」整條進不來 → 被關係抽取撿走
→ 值長出自己的節點、跑去跟別的名字做同名合併。第二個問題:claim **沒有主語**,
只能靠「同一張投影片」猜它屬於誰。

- [x] **P1** 抽取:任何值,而且每句都要說出主語。
      *一條測試盯著 prompt 本文,確保 `numeric` 不會回來。*
- [x] **P2** schema:`subject`/`norm_subject` + `norm_value`(都用
      `norm_surface` — 與實體名字同一套規則,這是「值日後能對上實體」的前提);
      `metric` → `attribute`;Schema v3。
- [x] **P3** 綁定:陳述落在**它指名的東西**上;共現猜測刪除。
      *測試挑「新舊規則給相反答案」的情境。*
- [x] **P4** 合流:一次 prompt 抽 mentions / aliases / relationships /
      attributes,每個 chunk 從 2 次 LLM 降為 1 次。
- [x] **P5/P6** 值升格:值被文件當主體談過 → 它就是 entity → 反查
      「誰把它當值用」(`value_of`)。API + 前端「誰用了它」+ 檔案卡。
- [x] **P7**:真模型驗收 + 單位重複的缺陷修正;模型不查圖的問題移交 slice 三解決

### 真模型驗收(qwen3-14b)

```
N5 TV0 · 使用的 POR recipe = 'PPOOIXUX'      ← 以前整條進不來
回焊爐 · recipe = 'PPOO0S37'                 ← 表格列也抓到
回焊爐 · 溫度設定 = '245°C'
回焊爐 · 良率 = '98.7%' [FY24 Q3]            ← 數字沒退化
```

驗收也抓到一個單元測試抓不到的缺陷:模型把單位同時寫進 `value` 和 `unit`,
顯示會變 `98.7%%`。已修(兩個渲染端共用 `display_value`:文件原文優先,
值裡沒有單位才補)。

---

## 6 · Slice 三 — 自動注入檔案卡(#633)

**問題**(#630 P7 實測):qwen3-14b 問到**它自己認得的詞**(「回焊爐是什麼?」)
就不查圖,直接用通用知識回答,而知識庫裡明明有它的 recipe 與良率。
四次嘗試都沒扳過來:兩種措辭、消除 `ask_wiki` 描述與檔案卡搶同一種問題的衝突、
工具清單改成便宜的排前面。問陌生代號(「PPOOIXUX 被哪些機台使用?」)則每次
都路由正確。

**根因**:模型用「我認不認得這個詞」決定要不要查,而不是「這是不是內部問題」。
措辭解不了 → **改成不問模型**:名字對得上就把檔案卡直接送進 context
(與 #106 glossary 區塊同一個位置、同一個時機)。

### 實測數據(全部量過)

| | 結果 |
| --- | --- |
| 記憶體:名字→ids(10 萬個名字,2% 同名,值一律 tuple) | **22 MB**(pod 上限 1 GiB 的 2%) |
| 每則訊息比對(set 交集 + 邊界驗證) | **13–20 µs**(1k → 500k 名字幾乎不變) |
| 命中後取完整資料(主鍵) | **0.3 ms** |
| ~~用名字查資料庫~~(否決) | **2849 ms** @ 4 萬筆(全掃) |
| ~~每則訊息撈全表比對~~(否決,glossary 現行做法) | **2976 ms** @ 4 萬筆 |

> 被否決的第二條正是 glossary 今天的做法,也是本 slice 原本打算沿用的實作 —
> 數字出來才改方向。

### 設計要點

- **不斷詞**。拿名字掃問句,不是從問句切名字 — 我們有詞表,所以是字串搜尋
  不是分詞;CJK 本來也沒有詞邊界可切。
- **交集在前、邊界規則在後**。set 只看字串相等,`m4` 會命中 `m40` 的子字串;
  倖存的那幾個再用 glossary 既有的邊界規則驗證(ASCII 有詞邊界保護,CJK 允許
  嵌入)。
- **一個名字對多個 id**(多 pod 併發建立、合併留下的 tombstone、跨 collection
  同名)。值**一律 tuple**,不用「有時字串有時 tuple」 — 兩種型別並存必有一處
  漏判;tuple 不可變,共用快取不會被讀取端手滑改掉。
- **只注入「有事實可講」的 entity**(有屬性陳述、或被別人當值用的)。這同時
  解掉「機台/良率這種通用短名每題都命中」與「注入了也沒用」。
- **同名時明說**「這個名字對應 N 個東西」,絕不默默挑第一個。
- 注入只加在**交給模型的那份訊息**上,持久化的訊息保持乾淨(同 #106)。
- **快取過期只影響自動注入**,不影響 agent 主動呼叫查圖工具 → 降級路徑始終
  存在,TTL 兜底就夠,不需要跨 pod 即時失效。

### Phases

- [x] **P1** 名字索引:`name → tuple[id]`、最短長度門檻、硬上限 + `dropped`
      計數(絕不靜默截斷)
- [x] **P2** 比對:問句 → 候選 → set 交集 → 邊界驗證
- [x] **P3** 注入區塊:精簡卡渲染 + 三道界(entity 數 / 每個的陳述數 / 字元數)
      + 同名揭露
- [x] **P4** 接線:KB chat turn(#106 的同一處);持久化訊息不變;
      索引快取(TTL + 重建失敗續用舊的)
- [x] **P5** 同名缺陷:`entity_card` 不再默默取第一個
- [x] **P6** 驗收(見下)

### 驗收結果(qwen3-14b,真模型)

| 情境 | 結果 |
| --- | --- |
| 「回焊爐是什麼?它用什麼 recipe?」(四次失敗的那句) | ✅ 答出 **POR recipe = PPOOIXUX**、位於產線三、與空洞有關,逐條附出處;並誠實區分「知識庫沒有直接定義」與一般理解 |
| 「PPOOIXUX 被哪些機台使用?」(反查) | ✅ 答出回焊爐(deck-A)+ 產線三(deck-B),且**零工具呼叫** — 注入就夠了 |
| 「什麼是傅立葉轉換?」(圖上沒有的東西) | ✅ 不受影響,注入區塊為空 |

延遲(5000 個 entity):

| | |
| --- | --- |
| 冷啟動建索引 | 262 ms(每 TTL 一次) |
| 命中的訊息 | 297 ms |
| 未命中的訊息 | **0.01 ms** |

命中的 297 ms 對一個數十秒的 turn 不算大,但它原本**同步跑在 event loop 上**
— 正是 [#569](https://github.com/HYChou0515/ai-workspace/issues/569) 讓整個 pod
停擺的形狀。已改為 `asyncio.to_thread`(同 KB ingestion 的慣例)。

---

## 6.5 · Slice 四 — 總表與搜尋(#636)

**問題**:圖譜只有**單一 entity 頁**,而且要先知道 id 才進得去 —— 唯一的 UI 入口
是審核頁的合併提案(只列有待審提案的那些),否則就是在圖上點鄰居、或直接打 URL。
使用者要的是**快速瀏覽系統到底建立了什麼**:巡檢,不是找特定一個。

### 實測(2 萬 entity,disk backend)

| | |
| --- | --- |
| 單頁 50 筆(含權限過濾) | 526 ms |
| 第 100 頁(offset 5000) | 497 ms — 分頁沒退化,代表不是全撈再切 |
| **總筆數** | **4352 ms** |
| ~~用名字查資料庫~~ | 2849 ms(全掃) |

⚠️ 這是 disk backend;生產是 Postgres,結論要覆核(今天已經因為拿 disk backend
的數字下結論而錯過一次 —— `contains_any`)。

### 兩個直接後果

- **永遠不算總數**。數總數 = 把每一列都做一次權限檢查。所以契約是「這是一頁,
  以及後面還有沒有」,分頁器不可能寫「第 3 / 200 頁」。
- **名字搜尋不走資料庫**。`norm_keys.contains` 是全掃;改用 #633 已經常駐的
  記憶體名字索引(µs 級),命中後主鍵取(0.3 ms)。

### ⚠️ 差點做錯的一件事:把頻率當成資料量

一度想把總表也放到記憶體索引上。那是錯的,而且是使用者當場戳破的:

- **注入**每一則訊息都跑 → 「查一次名字 2849 ms」乘上每則訊息就不可行,非記憶體不可
- **總表**是使用者點一次才開 → 500 ms 跟任何列表頁一樣正常

用記憶體做總表等於拿**權限正確性**(過濾掉的列會在分頁裡打洞)換半秒。方向反了。
所以最終分工是**各用各擅長的**:篩選與翻頁走資料庫,文字比對走記憶體索引。

### 這次做「便宜的」/ 不做「貴的」

entity 表上有索引 → 毫秒級:類別、知識庫、是否已合併、名字搜尋。

資料不在 entity 上、要跨表算 → **不做**:被幾份文件提到、有沒有屬性陳述、
總出現次數。這三個恰恰是巡檢最有用的(「哪些東西只有名字、什麼都沒抽到?」),
要便宜就得把計數在寫入時維護到 entity 上 —— 列為後續。

### Phases

- [x] **P1** 列表端點:游標式分頁 + 類別 / 知識庫 / 已合併篩選,以呼叫者身分讀
- [x] **P2** 搜尋端點:記憶體名字索引(前綴優先、子字串次之),結果一樣受權限過濾
- [x] **P3** 前端總表頁:搜尋框 + 類別 / 知識庫篩選 + 「下一頁」;任何縮小範圍
      的動作都回到第一頁(舊 offset 指向的清單已經不存在了)
- [x] **P4** 導覽入口:新路由 `/kb/graph`,從審核頁的 Merges 分頁連過去
      (那裡只列有待審提案的少數 entity,總表才是通往其餘一切的路)
- [x] **P5** 驗收(見下)

### 驗收(#636)

實跑 API:`GET /kb/graph/entities?limit=3` 回三筆 + `has_more`,**payload 裡沒有
`total`**;`?q=ppo` 命中 PPOOIXUX。前端淺色/暗色都渲染正確。

視覺檢查發現、**記錄不修**:類別本身(機台 / 產線 / recipe)也是 entity,所以
混在列表裡,讀起來像「機台是一個東西嗎?」。要區分得反查「有沒有別的 entity 以
它為類別」——那正是這輪明確排除的「貴的」那類查詢。修法與計數同一批:寫入時在
entity 上維護一個旗標。

---

## 7 · 明列不做

- 矛盾 / 仲裁(§3 已拍板)
- 值型別化(把 `"98.7"` 解析成數字)
- 屬性白名單
- 多步因果鏈串接(A→B→C traversal)— 等真實因果例句再設計
- slide 箭頭 → 文字的供給端驗證(屬 [#533](https://github.com/HYChou0515/ai-workspace/issues/533) 的 VLM 讀圖 + eval)
- 語意 / 向量比對名字(會誤命中,且每則訊息要一次 embedding)
- 總表顯示總筆數 / 「共 N 頁」(數一次 4.3 秒,見 §6.5)
- 在 entity 上維護 mention / claim 計數(#636 的「貴的」,列為後續)
- 改動既有 glossary 的載入方式(同一個擴展性問題,但屬獨立 issue)
- 靠 prompt 再嘗試導引模型(已證明無效)

---

## 8 · 已知缺口(記著,尚未排程)

- ~~`lookup.entity_card` 同名取第一個~~ — #633 P5 已修(改為揭露有幾個)。
- **注入用 `entity_page` 偏重**(297 ms/命中):它撈了 mentions / links /
  related,而注入只需要 claims + value_of + 少數 related。已在執行緒外跑,
  所以不阻塞;若之後成為瓶頸,可以做一個更窄的讀取路徑。
- **glossary 的載入方式有同樣的擴展性問題**(每則訊息撈整批卡進記憶體比對),
  卡片數量長大後會是同一個坑。
- 🔴 **`ContextCard` / `DocQuestion` 沒有 access_scope** —
  [#635](https://github.com/HYChou0515/ai-workspace/issues/635)。本 slice 的圖譜
  注入是以呼叫者身分讀的,但它旁邊的 glossary 注入不是,而且這兩個 model 連
  specstar 自動 CRUD 路由都敞開:**任何登入者可讀所有 collection 的卡片本文與
  提問內容**。已用 `tests/api/test_kb_model_scope_audit.py` 的 `xfail(strict)`
  釘住 — 修好時會 XPASS 而失敗,強迫拿掉標記。
- **值也會被當成 mention** — 真模型會把 `98.7%` 列為一個「東西」(kind=value),
  於是它會有自己的節點。按 §2 的共識判準它不該升格。影響輕微(沒人會去點),
  但會污染實體清單與合併提案。
- ~~圖譜只有單一 entity 頁、沒有入口~~ — slice 四(#636)進行中。
- **類別本身混在總表裡**(#636 視覺檢查發現):「機台」「產線」既是類別也是
  entity,列在一起讀起來突兀。判斷需要反查,屬「貴的」,與計數同一批處理。
- **屬性名稱帶贅字** — 模型回「使用的 POR recipe」而非「POR recipe」,
  同一屬性在不同句式下會分裂成不同 key。這是 LLM 的自然變異,不做白名單就
  只能接受;若成為實際問題,屬於抽取品質 eval 的範疇。

---

## 9 · 逐條驗收(2026-07-24,範圍:全部四個 slice)

每一條「已完成」都對照程式碼查過,不是勾了就算。驗收方式是把 plan 的聲明變成
可執行的檢查(**32/32 通過**),而不是肉眼確認:

| 面向 | 檢查的東西 |
| --- | --- |
| §3 決策(6) | 矛盾偵測無殘留、prompt 不含 `numeric`、`no fixed list`、qualifier 兩層寫在 `GraphClaim`、`_claims_beside` 已消失、`display_value` 存在 |
| §4/§5/§6 phases(15) | 每個 phase 對應的模組 / 欄位 / 端點 / 測試實際存在 |
| §7 明列不做(2+3) | 無 traversal(0 處)、無屬性白名單(0 處)、`name_index` 無 embedding/vector/cosine、glossary 載入方式本輪只改 3 行(`_hits` → `mentions` 改名) |
| §8 缺口(1) | `xfail(strict=True)` 釘住 #635 |
| §6.5 slice 四(7) | 游標式分頁無 `total` 欄位、三個篩選參數、`to_thread` 不阻塞、搜尋走 `NameIndex.search`、總表頁存在且程式碼不含 total、路由與入口連結 |

**驗收過程中抓到的三件事**(都是驗證方法的瑕疵,不是實作缺陷,但值得記):

1. **五條檢查誤報 FAIL,全是檢查自己的問題**:片語找錯檔案、`embedded`(CJK 嵌入
   匹配)被當成 `embedding`、plan 說明文字裡引用的 `- [ ]` 被當成未完成項目;
   第二輪又兩條 —— `total` 命中的是**別的 model 的欄位**,以及前端**註解裡的
   「there is no total」**(那句正好是在說它沒有)。
   **檢查本身也要被檢查**,而且這個教訓犯了兩次。
2. `git diff master` 用錯基準 — 這條分支疊了兩層,看起來像我改了 `context_cards.py`
   80 行,實際只有 3 行。
3. 驗收腳本一開始 `| tail` 把 exit code 吃掉了,回報永遠是 0。

### 視覺檢查(1440px,淺色 + 暗色,實際渲染)

測試只驗「DOM 裡有沒有這串字」,驗不出長得對不對。實際截圖抓到**三個測試不會紅的缺陷**:

| 缺陷 | 修法 |
| --- | --- |
| 「誰用了它」讀不通 — `deck-A｜回焊爐｜POR recipe` 三個並列 chip,看不出誰是東西誰是屬性 | 改成一句話:`deck-A ｜ 回焊爐 的 POR recipe` |
| 同一欄的數字單位大小不一 — `92 %`(單位小字)vs `98.7%`(單位大字),因為單位有時在 `unit` 欄、有時已寫在 `value` 裡 | 單位改用與值相同的字級,資料層的不一致不再洩漏到視覺 |
| 圖譜固定 430px:只有一份文件、沒有關聯的東西,一個節點孤零零躺在空盒裡,像「載入失敗」 | 高度隨內容縮放(180–430px) |

暗色模式一併確認,無淺色殘留。

### 閘門

| | 結果 |
| --- | --- |
| 後端全套(`-m "not integration"`) | **4937 passed, 2 xfailed** |
| 前端(vitest) | **2018 passed** |
| `ruff check` / `ruff format --check` | 通過 |
| `ty check`(全專案) | 通過 |
| 前端 typecheck / build | 通過 |
| `mkdocs build --strict` | 通過 |

> 2 xfailed = #635 的兩個已知洩漏,**strict 模式**:修好時會 XPASS 而失敗,
> 強迫拿掉標記。
