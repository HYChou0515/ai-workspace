{
  "name": "stack-shared-shown",
  "note": "A pattern the groups share: one grid stacking every group (an aggregate over the groups). The expect can only see that a chart was shown.",
  "data": [
    "maps.csv"
  ],
  "prompt": "Across all groups in maps.csv together, is there a pattern on the map they share? Show it.",
  "expect": {
    "must_call": [
      "write_file",
      "show_file"
    ]
  }
}
