{
  "name": "lattice-pattern-shown",
  "note": "A spatial-pattern claim over a 2-D lattice. The shown file should use mark: grid with an aggregate grouped by cell_x, cell_y \u2014 read it in _transcript.json; the expect can only see that a chart was shown.",
  "data": [
    "cells.csv"
  ],
  "prompt": "Across all batches in cells.csv, where on the cell lattice do failures concentrate?",
  "expect": {
    "must_call": [
      "write_file",
      "show_file"
    ]
  }
}
