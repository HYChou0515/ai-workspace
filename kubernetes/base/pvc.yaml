apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rca-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # storageClassName: ""   # set per-overlay/cluster if not using the default
