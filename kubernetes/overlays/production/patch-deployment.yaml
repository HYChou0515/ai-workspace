apiVersion: apps/v1
kind: Deployment
metadata:
  name: rca-app
spec:
  template:
    spec:
      containers:
        - name: rca-app
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "2"
              memory: "2Gi"
